I have identified the issue. The "backfilling" logic in `src/App.tsx` uses the project's display name (which may contain special characters like 'à') to update the library. If the internal file ID is sanitized (e.g. 'à' replaced by '_'), `updateLibrary` might fail to resolve the correct ID from the display name and fallback to re-computing a new ID, potentially creating a "Ghost File" (duplicate) instead of updating the existing one.

I propose to fix this by explicitly passing the known `fileId` to `updateLibrary` in the Smart Import logic.

### Plan:
1.  **Modify `src/App.tsx`**:
    - Locate the "Smart Import" logic where `existingBook` is found (around line 1160).
    - Update the `library.updateLibrary` calls to include `{ fileId: existingBook.fileId }` in the data payload.
    - This applies to both the fingerprint backfilling and the input language update, ensuring the system always writes to the correct file regardless of the display name's format.

This change directly addresses the risk of "naming convention discrepancy" you identified.