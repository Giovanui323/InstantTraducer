I will modify the auto-translation prefetching logic in `src/App.tsx` to prevent eager rendering of future pages when a preceding page is in error.

**Changes:**
1.  **File:** `src/App.tsx`
2.  **Logic:** In the `useEffect` responsible for auto-translation (around lines 1938-1954), locate the loop that processes `pagesToRequest`.
3.  **Modification:** Change the behavior when a page has an error (`hasError`). Instead of `continue` (which skips the error page and proceeds to queue the next ones), I will change it to `break`.
    *   **Current behavior:** If Page 1 is in error, it skips 1 and queues Page 2 and 3. This causes Page 3 to be rendered/processed even if Page 1 is blocked.
    *   **New behavior:** If Page 1 is in error, the loop breaks immediately. Page 2 and 3 are **not** queued. This stops the "speculative rendering" chain as requested, saving CPU/RAM and preventing obsolete work.

This strictly enforces a dependency where the system stops looking ahead if the current or immediate next page is in a failed state.