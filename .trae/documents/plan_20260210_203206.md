I will implement a comprehensive fix for the error propagation and user feedback issues.

### 1. Main Process: Safe Rename Handling
**Goal**: Prevent "File not found" errors and race conditions during renaming.
- **File**: `electron/projectHandlers.js`
- **Change**: Wrap the `rename-translation` handler logic inside the `writeSequencer`.
- **Reason**: This ensures that renaming never happens concurrently with a save operation (which also uses `writeSequencer`). It serializes file access for the specific `fileId`.

### 2. Renderer: Stop Queue on Critical Error
**Goal**: Prevent the system from processing pages when the file state is invalid.
- **File**: `src/hooks/useProjectManagement.ts`
- **Change**: In `scanAndRenameOldFiles` and `scanAndRenameAllFiles`:
    - If `window.electronAPI.renameTranslation` fails, immediately call `setIsPaused(true)`.
    - Trigger a critical Alert (via `showConfirm`) informing the user that the operation failed and the queue has been paused to prevent data loss.

### 3. UX Feedback: Toast Notification System
**Goal**: Inform the user when a save is blocked (Blacklist) instead of silently logging.
- **New Component**: `src/components/ToastNotification.tsx`
    - A lightweight, non-blocking notification component (bottom-center or top-right).
- **File**: `src/App.tsx`
    - Add state for `toast` ({ message, type, visible }).
    - Render `ToastNotification` when active.
    - Pass a `showToast` callback to `useAppLibrary`.
- **File**: `src/hooks/useAppLibrary.ts`
    - Accept `showToast` in arguments.
    - In `blockSave`, call `showToast` to notify the user ("Salvataggio bloccato temporaneamente per operazione in corso").
    - In `updateLibrary`, if blocked by blacklist, show a warning toast.

### 4. Implementation Steps
1.  **Create Toast Component**: Implement `ToastNotification.tsx`.
2.  **Update App.tsx**: Integrate the Toast system.
3.  **Update Hooks**: Wire up `showToast` in `useAppLibrary` and add pause logic in `useProjectManagement`.
4.  **Update Electron**: Apply `writeSequencer` to rename handler.