I will reduce the quality verification timeout to 45 seconds to prevent "zombie" timers and improve user experience.

### **Configuration Change**
- **File:** `src/constants.ts`
- **Action:** Update `AI_VERIFICATION_TIMEOUT_MS` from `180_000` (3 minutes) to `45_000` (45 seconds).
- **Reason:** As requested, a verification check that takes longer than 20-30 seconds is likely to fail. Reducing the timeout allows the user to retry sooner instead of waiting for a 3-minute timeout.
