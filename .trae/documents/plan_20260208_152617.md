I have analyzed the codebase and confirmed the presence of imports with `.tsx` extensions in `src/App.tsx` and `src/components/ReaderView.tsx`. This is permitted by the `"allowImportingTsExtensions": true` setting in your `tsconfig.json`, but as you noted, it is inconsistent with standard React/TypeScript conventions.

I will proceed to standardize these imports by removing the file extensions.

### Plan:
1.  **Update `src/App.tsx`**:
    - Change `import { ImageCropModal } from './components/ImageCropModal.tsx';` to `import { ImageCropModal } from './components/ImageCropModal';`
2.  **Update `src/components/ReaderView.tsx`**:
    - Change `import { OriginalPreviewModal } from './OriginalPreviewModal.tsx';` to `import { OriginalPreviewModal } from './OriginalPreviewModal';`
3.  **Verification**:
    - Run the build/type-check (if applicable) or verify no linting errors occur to ensure the changes don't break module resolution.
