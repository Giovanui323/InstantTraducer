I will address the "Scheduler Latency" issue by optimizing the background save strategy in `TranslationJobRunner.ts`. The diagnosis confirms that frequent, blocking I/O operations (saving the entire project file after every page) are causing the 11-second delays between pages.

**Plan:**

1. **Refactor** **`TranslationJobRunner.ts`**:

   * **Optimize** **`saveWithPriority`**: Implement a "Throttle" strategy instead of "Debounce". If a save timer is already active, do not reset it. This ensures that saves occur at a regular interval (e.g., every 30 seconds) rather than being postponed indefinitely or triggered too frequently.

   * **Fix Critical Save**: Ensure that `CRITICAL` priority saves include any accumulated `pendingSaveData` before clearing the timer, preventing potential data loss of pending background updates.

   * **Add** **`flushSave`** **method**: Extract the save flushing logic into a reusable method.

   * **Flush on Complete**: Ensure `flushSave` is called immediately when the queue becomes empty in `runLoop`, ensuring the final pages are saved without waiting for the timeout.

**Expected Outcome:**

* The system will save progress approximately every 30 seconds instead of after every page.

* This will drastically reduce the frequency of the "11-second" I/O blocking pauses.

* The "Scheduler" will pick up the next page almost immediately (within \~500ms) after the previous one finishes, solving the latency issue.

* Data safety is maintained by flushing on completion and periodically during execution.

