I will implement the following changes to address the model timeout and excessive cooldown issues:

1.  **Update Constants (`src/constants.ts`)**:
    *   Increase `AI_VERIFICATION_TIMEOUT_MS` from `60_000` (60s) to `120_000` (120s) to give `gemini-3-flash-preview` more time to respond.
    *   Add a new constant `GEMINI_TIMEOUT_COOLDOWN_MS` set to `60_000` (1 minute) to distinguish transient timeouts from quota exhaustion.

2.  **Update Service Logic (`src/services/geminiService.ts`)**:
    *   Modify `setModelCooldown` to accept an optional `durationMs` parameter.
    *   In `translateWithGemini` and `verifyTranslationQualityWithGemini`, when a **Timeout** occurs, use the new `GEMINI_TIMEOUT_COOLDOWN_MS` (1 min) instead of the default `GEMINI_COOLDOWN_MS` (20 mins).
    *   **Quota** errors will continue to use the standard 20-minute cooldown to respect API limits.

This approach resolves the immediate timeout errors by being more patient and ensures that if a timeout *does* occur, the system doesn't aggressively lock the model for 20 minutes, improving the user experience.