I will implement the requested 4-step fallback logic for Gemini translation models.

### 1. Update Constants
In `src/constants.ts`, I will define the new intermediate fallback model corresponding to "3 flash".
- Add `GEMINI_TRANSLATION_FAST_MODEL = 'gemini-3-flash-preview'`

### 2. Update Translation Service
In `src/services/geminiService.ts`, I will modify the error handling logic in `translateWithGemini` to follow the specified chain:
1.  **Primary**: `GEMINI_TRANSLATION_MODEL` ("3 pro")
    -   *Fallback on error* -> `GEMINI_TRANSLATION_FAST_MODEL` ("3 flash")
2.  **Secondary**: `GEMINI_TRANSLATION_FAST_MODEL` ("3 flash")
    -   *Fallback on error* -> `GEMINI_TRANSLATION_FALLBACK_MODEL` ("2.5 pro")
3.  **Tertiary**: `GEMINI_TRANSLATION_FALLBACK_MODEL` ("2.5 pro")
    -   *Fallback on error* -> `GEMINI_TRANSLATION_FLASH_MODEL` ("2.5 flash")
4.  **Final**: `GEMINI_TRANSLATION_FLASH_MODEL` ("2.5 flash")
    -   No further fallback (error is thrown)

I will also ensure the cooldown logic respects this chain where appropriate.