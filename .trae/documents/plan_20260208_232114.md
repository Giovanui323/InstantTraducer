I will fix the critical bug causing data collision between files. The issue consists of two parts: a frontend race condition and a backend logic error in the consolidation feature.

**Part 1: Fix Frontend Race Condition (`src/App.tsx`)**
The `doUpload` function sets the new project metadata *before* resetting the state. This causes the application to autosave the *old* project's annotations into the *new* project file before the state is cleared.
*   **Action:** Move `resetProjectState()` to the beginning of the `doUpload` function, ensuring the state is clean before any new project data is set.

**Part 2: Fix Backend Consolidation Logic (`electron/projectLogic.js`)**
The `performConsolidation` function aggressively groups files based on their `originalFilePath`. Since the application renames uploaded files to `original.pdf` internally, this causes *all* projects with the same page count to be treated as duplicates and merged ("consolidated"), overwriting data.
*   **Action:** Modify the grouping logic to prioritize `p.fileName` (the project name) over `originalFilePath`. This ensures that distinct projects (e.g., "Book A" and "Book B") are never merged, even if they share the same internal `original.pdf` filename.

**Verification:**
1.  **Frontend:** Confirm that uploading a new file clears the interface immediately and does not show annotations from the previous session.
2.  **Backend:** Confirm that the consolidation log no longer groups unrelated files (e.g., `name:original|pages:5`) and respects unique project names.

This two-pronged approach addresses both the immediate symptom (annotations appearing on new files) and the root cause (data corruption via consolidation).
