I will modify `src/utils/storageUtils.ts` to filter out frequent and repetitive `localStorage` reads from the logs.

The logs show excessive noise from `localStorage.getItem` calls for UI preference keys like `showThumbnail` and `auto_prefetch_pages`. These reads happen frequently during component rendering/mounting and clutter the logs, making it harder to debug actual issues.

**Plan:**

1. **Modify** **`src/utils/storageUtils.ts`**:

   * Define a list of keys to ignore in logs (e.g., `showThumbnail`, `auto_prefetch_pages`, `page_scale`, `reader_brightness`, etc.).

   * Update the `storage.getItem` method to skip logging if the requested key is in the ignore list.

   * (Optional) Apply the same logic to `sessionStorageWrapper.getItem` for consistency.

This change will silence the spammy logs while keeping logging enabled for other important storage operations and for `setItem` calls (which indicate actual state changes).

**Files to modify:**

* `src/utils/storageUtils.ts`

