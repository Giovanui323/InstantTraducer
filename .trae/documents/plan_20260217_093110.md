I will implement the model selection changes while ensuring **Translation** and **Verification** settings remain independent.

### 1. Update Gemini Model Definitions
- I will update `src/constants.ts` to include the complete list of new models (Gemini 3, Gemini 2.5, etc.) you provided.
- I will update `src/services/configValidation.ts` to recognize these models.

### 2. Refactor Settings UI (`SettingsModal.tsx`)
- **Translation Settings**:
    - Add the **"Modello Traduzione" Dropdown** synchronized with the "Modalità Veloce" checkbox.
    - **Crucial**: Ensure changing this **ONLY** updates the translation model (`geminiModel`) and **does not** change the verification model.
- **Verification Settings**:
    - Update the **"Modello Verifica" Dropdown** to also use the new list of models, giving you access to Gemini 3/2.5 for verification if desired.
    - Ensure the "Verifica Pro" checkbox continues to work as a quick toggle for the verification model, independently of the translation settings.
- **Decoupling**:
    - I will remove the code that automatically linked translation and verification settings, so your verification choice is preserved even if you change the translation mode.

### 3. Verification
- I will confirm that changing the Translation Model does **not** affect the Verification Model.
- I will verify that both dropdowns (Translation and Verification) offer the full list of new models.
