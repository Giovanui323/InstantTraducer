Ho capito. L'obiettivo è rendere il codice coerente con il comportamento attuale (Default 2, Max 4) senza alterare il funzionamento.

Ecco il piano definitivo:

1.  **Aggiornamento `src/constants.ts`**
    *   Sostituirò l'attuale `MAX_CONCURRENT_TRANSLATIONS` (che è ambiguo) con due costanti chiare:
        *   `DEFAULT_CONCURRENT_TRANSLATIONS = 2` (Mantiene il comportamento di base attuale).
        *   `MAX_ALLOWED_CONCURRENCY = 4` (Ufficializza il limite consentito dal SettingsModal).

2.  **Aggiornamento `src/components/SettingsModal.tsx`**
    *   Userà `DEFAULT_CONCURRENT_TRANSLATIONS` come valore preselezionato.
    *   Userà `MAX_ALLOWED_CONCURRENCY` per generare le opzioni del menu (1-4).

3.  **Aggiornamento `src/hooks/useAppTranslation.ts`**
    *   Userà `DEFAULT_CONCURRENT_TRANSLATIONS` come fallback se non ci sono impostazioni.
    *   Userà `MAX_ALLOWED_CONCURRENCY` come tetto massimo (sostituendo il 10 teorico con il 4 reale).

Questo intervento è puramente di refactoring: "pulisce" i nomi e i limiti nel codice per rispecchiare esattamente ciò che l'utente vede e usa oggi.
