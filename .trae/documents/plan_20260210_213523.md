I will modify `src/components/SettingsModal.tsx` to include `customPrompt` in the `areSettingsEqual` comparison function.

1. **Update** **`areSettingsEqual`**:

   * Add a check for `customPrompt`, treating `undefined` as an empty string (`''`) to ensure consistent comparison.

   * This ensures that when the prompt is saved as `undefined` (default) but the state sees it as `''`, they are considered equal, preventing desynchronization issues.

   * *Bonus*: I will also add the missing checks for `fastMode` and `proVerification` to ensure all settings are correctly tracked.

```typescript
// Proposed addition to areSettingsEqual
(a.customPrompt ?? '') === (b.customPrompt ?? '') &&
(a.fastMode ?? false) === (b.fastMode ?? false) &&
(a.proVerification ?? false) === (b.proVerification ?? false)
```

