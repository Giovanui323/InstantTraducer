I will optimize the initialization logic in `src/App.tsx` to prevent redundant `localStorage` writes at startup.

The current implementation uses `useEffect` hooks that trigger on component mount, writing the default (or loaded) values back to `localStorage` even if they haven't changed. I will modify these hooks to check the existing value in storage before writing.

**Specific changes:**
1.  **`verbose_logs`**: Add a check to compare the current storage value with the new `aiSettings.verboseLogs` value before calling `setItem`.
2.  **`reader_brightness`**: Add a check to compare `storage.getItem('reader_brightness')` with the current `brightness` state.
3.  **`reader_temperature`**: Add a check to compare `storage.getItem('reader_temperature')` with the current `temperature` state.
4.  **`translation_theme`**: Add a check to compare `storage.getItem('translation_theme')` with the current `translationTheme`.
5.  **`reader_column_layout`**: Add a check to compare `storage.getItem('reader_column_layout')` with the current `columnLayout`.
6.  **`page_scale`**: Add a check to compare `localStorage.getItem('page_scale')` with the current `scale`.

This approach directly addresses the "redundant writes" issue without requiring a major refactor of the settings management, ensuring backward compatibility and stability.