I have re-examined `src/hooks/useAppLibrary.ts` after your changes.

**Current State:**
1.  **`updateLibrary`**: The dependency array is `[setIsSaving, processSaveQueue]`. It does **not** include `recentBooks` or `currentProjectFileId`, so it is already stable.
2.  **`setCurrentProjectFileId`**: This function (lines 33-36) **still depends on `[recentBooks]`**, which is unnecessary and causes the `library` object to be recreated whenever `recentBooks` updates. This effectively recreates the context that `App.tsx` uses, which can trigger the loop.

**Updated Plan:**
I will proceed to stabilize the `useAppLibrary` hook by removing the unnecessary dependency.

1.  **Edit `src/hooks/useAppLibrary.ts`**:
    *   **Target**: `setCurrentProjectFileId` (lines 33-36).
    *   **Action**: Remove `recentBooks` from the dependency array. It should be `[]` (or just `[]` since it uses a ref and a stable setter).
    *   **Verification**: I will also double-check `updateLibrary` to ensure it remains clean.

This will break the cycle where `recentBooks` updates cause the `library` object (and its functions) to be recreated.
