I will modify the error handling logic in `useAppQuality.ts` to prevent discarding translations when the quality check fails due to API errors (Quota/Timeout).

**Detailed Implementation Plan:**

1. **Modify** **`src/hooks/useAppQuality.ts`**:

   * Locate the `processVerificationInternal` function.

   * In the `catch` block (lines 259-285), intercept errors identified as `RATE_LIMIT` (quota exceeded) or `NETWORK_ERROR` (timeout).

   * Instead of marking the verification as `failed` (which leads to the "paradox" of discarded work), mark the page as `verified` but with a warning summary (e.g., "Verifica saltata per risparmio quota").

   * This ensures the valid translation is preserved and the system moves on, rather than looping and wasting resources.

**Expected Outcome:**

* If the Quality Check fails due to a 429/Quota error, the translation will be **kept**.

* The UI will show the page as verified (possibly with a note), avoiding the "red" failed state.

* The system will not attempt to retry/discard the translation, breaking the infinite loop.

