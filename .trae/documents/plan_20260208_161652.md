I will modify `src/hooks/useTranslationQueue.ts` to implement the "skip after failure" policy.

1.  **Modify `pumpQueue` in `src/hooks/useTranslationQueue.ts`**:
    *   Locate the `isCircuitOpen(page)` block (around line 167).
    *   Replace the current "unshift and break" logic with "cleanup and continue".
    *   **Crucially**, I will ensure `queuedPagesRef.current.delete(page)` is called when skipping. This is the specific action that removes the page from the dependency check, allowing the next page to proceed.
    *   I will also clean up `forcedPagesRef`, `extraInstructionsRef`, and `completionCallbacksRef` to ensure no state is left behind ("rimetti a posto tutto").
    *   I will execute any pending `onComplete` callback for the skipped page (best effort).
    *   I will update `queueStats` to reflect the dropped page.

This ensures that when a page fails repeatedly (triggering the circuit breaker), it is completely removed from the queue system, unblocking the rest of the batch.