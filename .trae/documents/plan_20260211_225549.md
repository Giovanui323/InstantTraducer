I will implement the fixes for the three reported issues as follows:

### 1. Fix "Page 150" Hallucination (Prompt Engineering)
**File:** `src/services/verifierPrompts.ts`
- **Action:** Update `getVerifierStrictPrompt` to include a new strict rule about bibliographic references.
- **Detail:** Add a section explicitly instructing the AI to ignore page numbers in footnotes (e.g., "p. 150", "Idem, pag. 200") and treat them as external references, not missing content.

### 2. Fix Race Condition / Data Contamination
**File:** `src/hooks/useAppLibrary.ts`
- **Action:** Verify and confirm the implementation of `blockSave(oldId, 2000)` in `setCurrentProjectFileId`.
- **Detail:** It appears this fix may already be present in the provided file content. I will double-check and ensure the logic matches the requirement to create a 2-second safety shield when switching projects.

### 3. Break the Infinite Retry Loop
**File:** `src/services/translation/TranslationExecutor.ts`
- **Action:** Modify the `isSevere` check to allow saving if retries are exhausted.
- **Detail:**
  - Check `verificationResult?.postRetryFailed`.
  - If `postRetryFailed` is true, proceed to save the translation to disk even if `severity` is `severe`.
  - This ensures the user gets a result (marked as "Needs Review") instead of an endless spinner/discard loop.

### 4. Improve Loop Detection
**File:** `src/hooks/useAppQuality.ts`
- **Action:** Review `qualityAutoRetryCountRef` usage.
- **Detail:** Ensure the retry logic effectively prevents infinite loops, supporting the "Force Save" mechanism implemented in step 3.

I will perform these edits sequentially and verify the changes.