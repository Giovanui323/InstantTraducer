I will fix the application crash/loop issue by ensuring that the "Thinking" configuration is only sent to models that support it.

1. **Modify** **`src/services/geminiService.ts`**:

   * In `translateWithGemini`, inside the `runAttempt` function.

   * Update the `generateContentStream` configuration.

   * Add a conditional check: `const supportsThinking = model.includes("thinking") || model === GEMINI_TRANSLATION_MODEL;`

   * Only include `thinkingConfig` if `supportsThinking` is true.

   This change addresses the `400 Bad Request` error ("Thinking level is not supported") that occurs when the system falls back to `gemini-2.5-flash` or other fallback models. By stopping this error, the infinite retry loop will break, allowing the application to recover and correctly use any new API key you have saved.

   * <br />

