I will fix the "File Identity Instability" issue where the app forgets file fingerprints between restarts.

The root cause is in the backend's `get-translations` handler, which retrieves the list of projects but fails to include the stored `fingerprint` field in the returned data. This causes the frontend to think the fingerprint is missing, leading to unnecessary "Backfilling" and potential project duplication risks if files are moved.

**Plan:**
1.  **Modify `electron/projectHandlers.js`**: Update the `get-translations` handler to explicitly include the `fingerprint` field in the returned project object.
2.  **Verify the fix**: Ensure the code change correctly maps the `fingerprint` property from the stored JSON to the response object.

This change ensures that once a file's identity (fingerprint) is calculated and saved, it is correctly loaded on every app restart, maintaining stable file identity even if the file is moved or renamed.