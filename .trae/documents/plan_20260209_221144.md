Ecco il piano per risolvere l'incoerenza della qualità delle immagini e prevenire le "allucinazioni" dell'AI:

1.  **Modifica di `src/utils/pdfUtils.ts`**:
    *   **Aumento della Scala di Default**: Modificherò il valore predefinito di `scale` da `2.1` a `3.0` in tutte le funzioni di rendering (`renderDocPageToJpeg`, `renderDocPageWithFallback`, ecc.) per garantire una risoluzione di base più alta.
    *   **Revisione della Logica di Fallback**: Modificherò la funzione `renderDocPageWithFallback` per evitare il degrado aggressivo della qualità. Invece di scendere a `scale: 1.0` e `quality: 0.5`, la nuova strategia sarà:
        *   **Tentativo 1**: `scale: 3.0`, `quality: 0.86` (Standard elevato)
        *   **Tentativo 2**: `scale: 3.0`, `quality: 0.80` (Mantiene la risoluzione, riduce leggermente la qualità JPEG)
        *   **Tentativo 3**: `scale: 2.5`, `quality: 0.75` (Riduzione minima, ancora molto leggibile per l'OCR)
    *   Questo assicurerà che anche in caso di retry, l'immagine inviata a Gemini rimanga sopra la soglia critica di leggibilità (~200KB per pagina A4).

2.  **Verifica**:
    *   Non posso testare direttamente la generazione con Gemini senza API key, ma verificherò che il codice compili correttamente e che la logica di fallback sia implementata come descritto.

Se sei d'accordo, procederò con queste modifiche.