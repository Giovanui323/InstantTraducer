Based on the logs provided, the `gemini-3-flash-preview` model is consistently taking longer than 45 seconds to verify pages, triggering the `AI_VERIFICATION_TIMEOUT_MS` limit. This results in the model being placed in cooldown and forcing a fallback to `gemini-2.5-flash`, which causes delays.

To resolve this issue and prevent the system from flagging the model as "unreliable" simply because it's slow, I propose the following changes:

1.  **Increase Verification Timeout**:
    *   File: `src/constants.ts`
    *   Action: Increase `AI_VERIFICATION_TIMEOUT_MS` from `45_000` (45 seconds) to `90_000` (90 seconds).
    *   **Reasoning**: 45 seconds is often insufficient for Vision models to analyze a full page and a translation. Doubling this threshold will allow `gemini-3-flash-preview` to complete its task without timing out, thus avoiding the cooldown and fallback loop.

2.  **Review Cooldown Settings**:
    *   The system already has a cooldown mechanism (`GEMINI_TIMEOUT_COOLDOWN_MS` = 1 minute).
    *   I will leave this as is, because increasing the timeout should prevent the cooldown from being triggered in the first place.

This approach directly addresses the "Constant Timeouts" and the resulting "delays" mentioned in your request.
