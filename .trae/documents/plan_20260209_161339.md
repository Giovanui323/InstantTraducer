I will update `src/services/geminiService.ts` to ensure 503 errors (Service Unavailable) trigger the fallback mechanism for the quality verification step.

**Specific changes:**
1.  **Modify `isQuotaError` helper (or add a new `isFallbackableError` helper):**
    *   Add checks for status code `503`.
    *   Add checks for error messages containing "service unavailable", "overloaded", or "capacity".
2.  **Update `verifyTranslationQualityWithGemini`:**
    *   Ensure the catch block detects these 503 errors and triggers the fallback to `GEMINI_VERIFIER_FALLBACK_MODEL` (`gemini-2.5-flash`), just like it does for quota limits or timeouts.

This will ensure that when `gemini-3-flash-preview` fails with a 503 error, the system automatically retries with the stable fallback model.