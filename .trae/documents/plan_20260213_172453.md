I will fix the "Infinite Lock" issue and improve error handling for blocked saves as requested.

**Diagnosis**
1.  **Infinite Lock**: `blockSave` in `useAppLibrary.ts` clears local state after a timeout but fails to notify the backend, leaving `WriteSequencer` blocked indefinitely.
2.  **Log Spam**: `processSaveQueue` treats `BLOCKED_BY_BACKEND` as a generic critical error, causing unnecessary noise when saves are legitimately (temporarily) blocked.

**Proposed Changes**

1.  **Modify `src/hooks/useAppLibrary.ts` (Fix `blockSave`)**:
    *   In the `setTimeout` callback within `blockSave`, add `window.electronAPI?.unblockSave(fileId)`.
    *   This ensures backend synchronization when the local block expires.

2.  **Modify `src/hooks/useAppLibrary.ts` (Improve `processSaveQueue`)**:
    *   Update the save response handling logic to explicitly check for `res?.error === 'BLOCKED_BY_BACKEND'`.
    *   If blocked, throw a specific error message (or handle it) that can be identified in the catch block.
    *   In the `catch` block, check for this specific error. If found, log it as a warning (e.g., `log.warning`) instead of `log.error`, and allow the existing retry logic to reschedule the save.

**Verification**
I will ensure that the code correctly calls the unblock IPC and that the error handling logic distinguishes between critical failures and temporary blocks, reducing log noise while maintaining data safety.