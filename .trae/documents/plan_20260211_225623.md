Based on the analysis of the current codebase, I have found that the **Backend**, **IPC**, and **Bridge** components (Steps 1-3) are **already implemented** and align with your requirements. The missing piece is the **Frontend integration** (Step 4), which is critical to actually triggering the blocking mechanism.

My plan is to focus on connecting the frontend to the existing backend infrastructure.

### 1. Frontend: Integrate with Library Hook (`src/hooks/useAppLibrary.ts`)
- **Update `blockSave`**:
  - Call `window.electronAPI.blockSave(fileId)` to notify the main process.
  - Ensure this is handled without blocking the UI (fire-and-forget or non-blocking await).
- **Update `unblockSave`**:
  - Call `window.electronAPI.unblockSave(fileId)` to release the backend block.
- **Verify**:
  - Ensure the calls are placed correctly within the existing `useCallback` hooks.

This will complete the "Zombie Save" fix by ensuring that when the frontend blocks a project (e.g., during a switch), the backend is explicitly told to reject any incoming or pending writes for that ID.