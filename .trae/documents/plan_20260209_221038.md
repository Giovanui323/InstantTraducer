I will fix the "Ghost Timeout" issue where the software declares failure too early (120s) while Gemini is still processing (response arriving at 178s).

**1. Update Timeout Constants (`src/constants.ts`)**
*   Increase `GEMINI_FIRST_CHUNK_TIMEOUT_MS` to `240_000` (240 seconds / 4 minutes) to accommodate slower Gemini responses (like the 178s case).
*   Increase `GEMINI_FIRST_CHUNK_WARNING_MS` to `210_000` (210 seconds) to align with the new timeout.

**2. Fix Logic in `src/services/geminiService.ts`**
*   **Remove Hardcoded Limit:** Replace the hardcoded `120` seconds check in the heartbeat loop with the dynamic `GEMINI_FIRST_CHUNK_TIMEOUT_MS / 1000`.
*   **Align Warning:** Update the warning log to reflect the actual timeout value instead of "120s".
*   **Adjust Progress Messages:** Update the intermediate progress message threshold (currently `> 60s`) to be proportional to the new timeout (e.g., `> 90s`), giving the user better feedback during long waits.

This ensures the software waits long enough for the response, eliminating the false "Failure" message and preventing potential duplicate requests/billing.