I will fix the issue where file renaming fails when the window is not focused (e.g., when the user switches to another app) by implementing the following changes:

1.  **Disable Background Throttling in Electron**:
    *   I will modify `electron/main.js` to set `backgroundThrottling: false` in the `webPreferences`. This ensures that the application continues to run at full speed even when it's in the background, preventing IPC (Inter-Process Communication) interruptions that are critical for the file renaming process.

2.  **Optimize Window Focus Handling in React (`src/App.tsx`)**:
    *   I will update the `scanDims` function (which logs the "Pausa scansione..." message) to use `useRef` for tracking window focus state instead of React state dependencies.
    *   This change will prevent the background scanning process from constantly restarting (mounting/unmounting) whenever the user switches windows. Instead, it will gracefully check the current focus state without interrupting the component lifecycle, reducing the risk of race conditions or resource conflicts during critical operations like renaming.

These changes directly address the user's hypothesis about IPC interruption and the observed log message.
