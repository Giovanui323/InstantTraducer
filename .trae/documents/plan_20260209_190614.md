I will implement the **Pro Verification Mode** as requested, ensuring the logic is modular to avoid bloating existing files.

### **1. Constants & Types**
*   **`src/constants.ts`**: Add `GEMINI_VERIFIER_PRO_MODEL` (likely `gemini-3-pro-preview`).
*   **`src/types.ts`**: Update `AISettings` to include `proVerification?: boolean`.

### **2. New Logic Module (to keep files light)**
*   **Create `src/services/geminiModelLogic.ts`**:
    *   Centralize model selection logic here.
    *   Move `getNextFallbackModel` (translation) here.
    *   Implement **new** `getNextVerifierFallbackModel(currentModel)` logic:
        *   If `Pro` -> fallback to `Flash`.
        *   If `Flash` -> fallback to `Flash Fallback` (2.5).

### **3. Service Update**
*   **`src/services/geminiService.ts`**:
    *   Refactor to use `geminiModelLogic.ts`.
    *   Update `verifyTranslationQualityWithGemini` to support the new fallback chain (Pro -> Flash -> Flash 2.5).

### **4. State Management**
*   **`src/hooks/useAiSettings.ts`**:
    *   Initialize `proVerification` (default `false`).
    *   Update the auto-configuration logic: if `proVerification` is enabled, set the verifier model to `GEMINI_VERIFIER_PRO_MODEL`.

### **5. UI Implementation**
*   **`src/components/SettingsModal.tsx`**:
    *   Add a new toggle "Verifica Pro (Alta Qualità)" in the Gemini section.
    *   Place it near the "Fast Mode" toggle for a cohesive "Performance vs Quality" control area.
