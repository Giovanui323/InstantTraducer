I will modify `src/App.tsx` to fix the "State Conflict during Switching" and "Project Racing" issues.

**Fix Details:**

1. **Abort Verification Queue:** In `resetProjectState`, I will add a call to `quality.abortAllVerifications()`. This ensures that any running quality checks (like the one on Page 6) are immediately cancelled when switching projects, preventing them from updating the state or logs of the new project.
2. **Verify Project Switching Lock:** I will confirm that the `openingProjectRef` lock in `handleOpenProject` is correctly implemented to handle the "Duplicate Request Race" warnings observed by the user. The existing warnings in the log indicate the lock is working, but I will ensure the critical section is properly protected.

**Files to Modify:**

* `src/App.tsx`

**Verification:**

* I will verify that `quality.abortAllVerifications` is correctly called during project reset.

* I will verify that `resetProjectState` is called during `handleOpenProject`.

