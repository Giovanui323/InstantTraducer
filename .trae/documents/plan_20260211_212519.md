I have analyzed the codebase and confirmed the user's hypothesis:
1.  **Redundancy Confirmed**: The `ReaderView` and `OriginalPreviewModal` components render pages directly from the PDF file using `pdfjs-dist` and `<canvas>`. They **do not require** the saved `source-pX.jpg` files for display (except as a potential fallback, but the PDF rendering is the primary path).
2.  **Inefficiency Located**: In `TranslationExecutor.ts`, when a page is re-translated, the system generates a JPEG image from the PDF solely to send it to the Gemini API. However, it currently saves this temporary image to disk via `saveSourceForPage`, which is unnecessary I/O and disk usage.
3.  **Safe to Remove**: Since the UI renders from the PDF on demand, removing this disk save operation will not break the "View Original" feature. The image will still be kept in memory (`setOriginalImages`) for the current session's UI, satisfying any immediate feedback needs.

**Plan:**
1.  **Modify `src/services/translation/TranslationExecutor.ts`**:
    *   Locate the `executePageTranslation` function.
    *   Find the fallback block where the page is rendered from the PDF (approx. line 208).
    *   **Remove or Comment Out** the `services.saveSourceForPage` call (approx. lines 271-273).
    *   This ensures images generated for AI translation are held in memory for the API call but not persisted to disk, resolving the inefficiency.
    *   (Optional) I will also apply the same logic to the "Replacement Page" rendering block (approx. line 181) for consistency, as those are also re-renderable.

This change directly addresses the "Potential Inefficiency" by eliminating the disk write and storage of temporary images.