I have analyzed the logs and codebase and prepared a plan to address the 3 performance tasks.

### **Task 37: Redundancy in Project Switching**

**Goal**: Prevent duplicate switching and ensure idempotency.

* **Fix in** **`src/App.tsx`**:

  * Modify `handleOpenProject` to strictly check `currentProjectFileId` against the new `fileId` to prevent reloading the active project.

  * Use `openingProjectRef` as a lock to ignore duplicate requests (Debouncing).

  * Ensure `resetProjectState` is only triggered when a valid, new switch is confirmed.

### **Task 38: Session Close Lifecycle**

**Goal**: Ensure saving the old project doesn't block the new one and fix the 10s timeout.

* **Fix in** **`src/hooks/useAppLibrary.ts`**:

  * Modify `blockSave` to use a shorter, dynamic timeout (e.g., 2000ms instead of fixed 10s) since we await `flushSaves` synchronously.

  * Ensure `unblockSave` correctly clears both "blacklisted" and "transitioning" states immediately.

* **Fix in** **`src/App.tsx`**:

  * In `handleOpenProject`, call `library.unblockSave` immediately after the new project is successfully loaded to unlock the UI.

  * Ensure `flushSaves` is awaited before starting the new load to prevent "Collision of Saves".

### **Task 39: Prevention of "Rendering Storm"**

**Goal**: Prevent UI freeze due to mass rendering and fix "Original PDF not found".

* **Fix in** **`src/App.tsx`**:

  * **Stop Rendering**: In `resetProjectState`, explicitly call `setPdfDoc(null)` and clear `canvasRefsMap` (if accessible) to immediately stop the old PDF from rendering during the switch.

  * **Fix "Original PDF Not Found"**: In `handleOpenProject`, add a retry mechanism (e.g., 3 attempts with 500ms delay) for `getOriginalPdfPath` to handle race conditions where the file path isn't ready yet.

  * **Memory**: Revoke Blob URLs in `resetProjectState` (already present, but will verify coverage).

### **Verification Plan**

1. **Switching Test**: Open a project, then immediately click it again. Verify in logs that "Ignoro ricaricamento" appears.
2. **Rapid Switch**: Open Project A, then immediately Project B. Verify "Switching to" happens once and no "Save blocked" errors persist.
3. **Render Test**: Open a large PDF. Verify logs don't show 7-8 simultaneous render requests and the UI remains responsive.

