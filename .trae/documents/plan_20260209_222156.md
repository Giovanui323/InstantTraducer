I will implement a robust retry mechanism for metadata extraction and increase the page context to 5 pages as requested.

### 1. Enhance Metadata Extraction (Backend Logic)
**File:** `src/services/geminiService.ts`
- **Current:** Simple one-time fallback from Pro to Flash model.
- **New:** Implement a robust retry loop using the `retry` utility with exponential backoff.
- **Logic:**
    1.  Attempt with Primary Model (e.g., Gemini 1.5 Pro).
    2.  If it fails (Quota/Timeout), retry up to 3 times with backoff.
    3.  If it still fails, switch to Fallback Model (e.g., Gemini 2.0 Flash).
    4.  Retry Fallback Model up to 3 times with backoff.
    5.  This ensures transient quota errors don't cause the "Skip Rename" issue.

### 2. Increase Context for Better Accuracy (Frontend Logic)
**File:** `src/App.tsx`
- **Current:** Generates thumbnails for the first **3** pages during upload.
- **New:** Increase this to the first **5** pages.
- **Benefit:** Sending 5 pages to the AI (as requested) significantly improves the chance of finding the Title/Author if they aren't on the first page, and the robust retry logic will now use this richer context.

### 3. Verification
- I will verify the changes by checking the code structure and ensuring the `retry` utility is correctly integrated.
- Since I cannot upload a physical file to trigger the quota error, I will rely on the code correctness of the retry logic which uses standard status codes (429, 503).
