I will add a **Hand Tool** (panning functionality) to the `OriginalPreviewModal` component. This will allow users to move the page around by clicking and dragging, especially when zoomed in, which addresses your request to "have something like the hand" when clicking on the thumbnail.

### Plan

1.  **Modify `src/components/OriginalPreviewModal.tsx`**:
    *   **Imports**: Import the `Hand` icon from `lucide-react`.
    *   **State Management**:
        *   Add `isHandToolActive` state (boolean) to toggle the tool.
        *   Add `isPanning` state (boolean) to control the cursor style (grab vs grabbing).
        *   Add a `panStateRef` to track pointer events and scroll positions efficiently without re-renders.
    *   **Toolbar Update**:
        *   Add a new button for the Hand tool in the sticky toolbar (top right).
        *   Style it consistently with existing buttons, highlighting it when active.
    *   **Interaction Logic**:
        *   Implement `onPointerDown`, `onPointerMove`, `onPointerUp`, and `onPointerLeave` handlers on the main container.
        *   These handlers will manage the scrolling logic based on mouse/touch movement when the hand tool is active.
        *   Add logic to prevent the modal from closing (`onClose`) if a drag action was performed (distinguishing between a click to close and a drag to move).
    *   **Styling**:
        *   Apply `cursor-grab` when the hand tool is active.
        *   Apply `cursor-grabbing` while dragging.

### User Experience
*   When you open the original page preview (by clicking the thumbnail), you will see a "Hand" icon in the toolbar.
*   Clicking it enables the Hand tool.
*   You can then click and drag anywhere on the screen to move the image around (pan), which is very useful when zoomed in.
*   Clicking without dragging will still close the modal (if clicking on the background) or behave normally.
