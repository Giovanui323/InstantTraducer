I have identified the cause of the issue. The text selection logic in `src/utils/textSelection.ts` contains an "optimization" that incorrectly resets the offset calculation when selecting text inside the notes section.

When you select text in the notes:
1.  The code detects that the selection is inside a complex container (the notes list).
2.  It calculates the character offsets relative to *that container* (the notes list) instead of the entire document.
3.  As a result, the highlight is saved with a small offset (e.g., 0-10), causing it to appear at the very beginning of the document instead of in the notes.

To fix this, I will modify `src/utils/textSelection.ts` to ensure that offsets are always calculated relative to the entire document container, regardless of whether the selection is inside a nested structure like the notes list.

### Plan:
1.  **Modify `src/utils/textSelection.ts`**:
    *   Update `computeOffsets` to remove the logic that narrows the scope to `containingChild`.
    *   Update `computeOffsetsWithContext` to remove the same logic.
    *   This will force the offset calculation to always start from the root container, ensuring correct global offsets for notes.

This change is safe and will enable highlighting within notes (and any other nested structures) to work correctly.