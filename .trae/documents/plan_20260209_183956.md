I will implement a "Last Resort" fallback mechanism using `gemini-1.5-flash`, which has generous rate limits, to handle cases where `gemini-2.5-flash` is exhausted.

### **1. Update Constants**
*   **File**: `src/constants.ts`
*   **Action**: Add `GEMINI_FLASH_LEGACY_MODEL = 'gemini-1.5-flash'` to serve as the final fallback for both translation and verification.

### **2. Enhance Verification Service**
*   **File**: `src/services/geminiService.ts`
*   **Function**: `verifyTranslationQualityWithGemini`
*   **Logic**:
    *   Modify the error handling block (around line 735) to support a 3-step fallback chain:
        1.  `gemini-3-flash-preview` (Primary)
        2.  `gemini-2.5-flash` (Fallback)
        3.  `gemini-1.5-flash` (Last Resort)
    *   If the current model is `gemini-2.5-flash` and fails with a quota error, retry with `gemini-1.5-flash`.

### **3. Enhance Translation Service (Proactive)**
*   **File**: `src/services/geminiService.ts`
*   **Function**: `translateWithGemini` & `getNextFallbackModel`
*   **Logic**:
    *   Update `getNextFallbackModel` to return `gemini-1.5-flash` when `gemini-2.5-flash` is passed.
    *   Update the `translateWithGemini` fallback condition (lines 240 & 572) to allow falling back *from* `gemini-2.5-flash` *to* `gemini-1.5-flash` before giving up.

### **4. Verification**
*   I will verify the changes by checking the fallback logic flow in the code (static analysis) and ensuring no type errors are introduced.
