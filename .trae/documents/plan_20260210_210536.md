I will implement a "Cleanup" feature for the Deep Health Check to resolve the detected "orphaned assets" (orphaned folders).

### **1. Backend: Implement Cleanup Logic**
**File:** `electron/fileUtils.js`
- Create a new function `cleanupOrphanedAssets()`.
- This function will:
    1.  Re-run the scan to identify orphaned asset folders (ensuring safety).
    2.  Move these folders to the **Trash** (using a `trash_orphaned_TIMESTAMP_NAME` format) instead of permanent deletion, allowing for recovery if needed.
    3.  Update the `inconsistencyTracker` state.
    4.  Return the count of cleaned items.

### **2. Backend: Create IPC Handler**
**File:** `electron/healthMonitoring.js`
- Register a new IPC handle `cleanup-orphaned-assets` that calls `cleanupOrphanedAssets()`.

### **3. Preload: Expose API to Frontend**
**File:** `electron/preload.cjs`
- Add `cleanupOrphanedAssets: () => ipcRenderer.invoke('cleanup-orphaned-assets')` to the `electronAPI`.

### **4. Frontend: Add UI Controls**
**File:** `src/components/SettingsModal.tsx`
- Update the **Diagnostics** section to:
    1.  Display the `orphanedAssets` count (currently hidden).
    2.  Show a **"Pulisci" (Clean)** button next to the count if `orphanedAssets > 0`.
    3.  Connect the button to the new API and refresh the health report upon completion.

### **Verification**
- I will verify that the button appears when orphans are detected.
- I will verify that clicking the button moves the folders to the trash and updates the count to 0.
