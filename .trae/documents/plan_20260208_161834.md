I will modify the translation dependency logic to resolve the inconsistency between high concurrency settings and sequential context blocking.

**Proposed Changes:**
- **File**: `src/hooks/useAppTranslation.ts`
- **Logic Change**: In the `checkDependency` function, I will update the condition to allow pages to start immediately if `translationConcurrency` is greater than 1, regardless of the `sequentialContext` setting.
  - Currently: Checks `!sequentialContext && concurrency > 1`.
  - New Logic: Checks `concurrency > 1 || !sequentialContext`.
- **Effect**:
  - If you set Concurrency to 2+, pages will send in parallel without waiting for the previous page to finish (Sequential Context will be ignored for speed).
  - If you set Concurrency to 1, Sequential Context will still be respected (waiting for page N-1 before starting N) unless explicitly disabled.

This directly addresses your request: "quando è 2 o superiore deve inviare senza aspettare".