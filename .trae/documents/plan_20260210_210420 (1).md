I have identified the cause of the chaotic queue order in `src/App.tsx`.

The current implementation of "Traduci tutto" (Translate All) specifically reorders the pages to prioritize the **current view**:
1. It starts from the `currentPage` and goes to the last page.
2. Then it wraps around and adds pages from 1 to `currentPage - 1`.

This explains why you see pages 9-20 added first, and Page 1 added last (if you were on page 9). As you correctly noted, this creates a bottleneck because Page 1 is a dependency for the sequential context of all subsequent pages.

### Plan
I will modify `src/App.tsx` to ensure a strict ascending order (1 to N) when "Traduci tutto" is clicked.

1.  **Edit `src/App.tsx`**:
    *   Locate the `useEffect` handling `isBatchProcessing` (around line 2141).
    *   Remove the split loop that reorders pages based on `startPage`.
    *   Replace it with a single loop that iterates from `1` to `total`.

This change will ensure that Page 1 is always queued first (if not already done), unblocking the sequential context flow for the rest of the document.