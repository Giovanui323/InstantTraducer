I will modify the application logic to ensure "Avvia Subito" (Start Now) works immediately and provides context even when previous pages are not translated.

### 1. Unblock "Avvia Subito" Queue Execution
I will modify `src/hooks/useAppTranslation.ts` to update the `checkDependency` function.
- **Current Behavior**: The queue checks for sequential dependencies (previous page translated) even if the translation is forced, causing the "Start Now" request to wait in the queue if previous pages are missing.
- **New Behavior**: I will add a check `if (isForced) return true;` at the beginning of the dependency check. This ensures that any page triggered with "Avvia Subito" bypasses the sequential dependency requirements and starts immediately.

### 2. Provide Original Text as Fallback Context
I will modify `src/services/translation/TranslationExecutor.ts` to handle missing context.
- **Current Behavior**: If the previous page is not translated, `prevContext` is sent as an empty string.
- **New Behavior**: If the translated text for the previous page is missing, I will fetch the **original text** from the PDF source (using `pdfDoc.getPage(targetPage - 1).getTextContent()`) and send that as the context.
- **Implementation**:
    - Inside `executePageTranslation`, I will check if `context.translationMap[targetPage - 1]` is empty.
    - If empty and `targetPage > 1`, I will retrieve the text content of the previous page from the PDF.
    - This ensures the AI has textual context (the original source text) to maintain continuity, satisfying the user's requirement ("se prima non è tradotto manda solo il testo originale").

### Summary of Changes
- **`src/hooks/useAppTranslation.ts`**: Allow forced pages to bypass dependency checks.
- **`src/services/translation/TranslationExecutor.ts`**: Fallback to extracting original PDF text for the previous page when its translation is not available.