I will implement a comprehensive fix for the deadlock and the forced enqueue loop.

1.  **Deadlock Fix (Concurrency Bypass)**:
    *   **Goal**: Allow verification tasks triggered by an active translation job to bypass the global concurrency limit, as the parent translation job already holds a slot.
    *   **`src/hooks/useVerificationQueue.ts`**: Update `enqueueVerification` and the queue worker to accept a `bypassConcurrency` option. If true, `globalConcurrency.acquire()` and `release()` will be skipped.
    *   **`src/hooks/useAppQuality.ts`**: Update `verifyAndMaybeFixTranslation` to pass the `bypassConcurrency` flag to the verification queue.
    *   **`src/services/translation/TranslationExecutor.ts`**: Pass `bypassConcurrency: true` when calling `verifyAndMaybeFixTranslation`, ensuring that the verification step doesn't wait for a new slot while holding an existing one.

2.  **Loop Fix (Redundant Enqueue Prevention)**:
    *   **Goal**: Prevent infinite loops where "forced" enqueues (e.g., auto-retries) repeatedly kill and restart the same job with identical parameters.
    *   **`src/hooks/useTranslationQueue.ts`**:
        *   Add `activeTranslationParamsRef` to track parameters (`extraInstruction`, `translationModelOverride`) of currently running jobs.
        *   In `enqueueTranslation`, if `force: true` is requested, check if the page is already active or queued with *identical* parameters. If so, ignore the request.
        *   Update `pumpQueue` to populate and clean up `activeTranslationParamsRef`.

This two-pronged approach ensures that slots are released correctly (avoiding 2/2 stuck state) and that the system doesn't enter a self-sustaining retry loop.
