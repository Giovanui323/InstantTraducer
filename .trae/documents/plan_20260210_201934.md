I will modify `src/hooks/useAppTranslation.ts` to remove the artificial concurrency limit when "Sequential Context" is enabled.

**The Fix:**

1. **Remove the forced limit**: Currently, `useAppTranslation.ts` overrides your `translationConcurrency` setting (2) and forces it to 1 whenever `sequentialContext` is active (default). I will remove this override.
2. **Let the Queue manage it**: The `useTranslationQueue` already handles dependencies smarty. If `sequentialContext` is on, it will naturally serialize page 1 then page 2.
3. **Result**:

   * Your settings (Concurrency: 2) will be respected in the logs.

   * The "Concurrency limit reached (1/1)" error will disappear.

   * If pages *must* wait for each other (because of Sequential Context), the logs will correctly say "Pages waiting for dependencies" instead of "Concurrency limit reached".

   * If you disable Sequential Context, you will immediately get true parallel translation (2 at a time).

This resolves the discrepancy between your settings and the system behavior, while maintaining the safety of sequential context when needed.

**Files to modify:**

* `src/hooks/useAppTranslation.ts`: Remove the ternary operator limiting concurrency.

