I have analyzed the code and found the answers to your questions.

### 1. "Critical Mode" Activation vs. Legal Context
**Why you only see the log during retries:**
The log `[DEBUG] CRITICAL MODE ACTIVE` is inside a conditional block `if (isRetry)` in `src/services/geminiService.ts`.
- **Legal Context:** This is correctly applied to **every** request (first attempt and retries) if enabled in settings. It injects specific instructions (like "Arrêt" = "Sentenza") into the *System Prompt*.
- **Critical Mode:** This is a separate mechanism that only triggers on retries. It adds a "force correction" block to the *User Prompt*.

**Conclusion:** The instructions are being sent, but you don't see them in the logs because the logging is suppressed for non-retry attempts.

### 2. Inheritance of Extra Instructions
**Do correction instructions propagate to other pages?**
**No.** I verified the queue logic in `src/hooks/useTranslationQueue.ts`.
- Instructions are stored in a `ref` keyed by page number: `extraInstructionsRef.current[page]`.
- When a page is processed, the instruction is retrieved and **immediately deleted**: `delete extraInstructionsRef.current[page]`.
- There is no mechanism for these instructions to "leak" to the next page in the queue.

### Proposed Solution
To resolve the ambiguity and give you better visibility, I propose:

1.  **Improve Logging:** Modify `src/services/geminiService.ts` to always log the System Prompt (or at least the "Legal Context" status) in debug mode, not just during retries.
2.  **Strengthen Legal Context:** (Optional) If you feel the first attempt ignores the legal terminology, we can move the Legal Context instructions from the *System Prompt* to the *User Prompt* (or repeat them there) to give them more weight.

I will proceed with **Step 1 (Improved Logging)** to let you verify the Legal Context activation. Shall I also implement Step 2?