I will implement a robust "Immediate Save" mechanism to eliminate the data integrity risk ("The Periodic Save Gamble") by ensuring that every completed page translation triggers an immediate save to disk, rather than waiting for a background timer.

### 1. Update `TranslationExecutor.ts`
*   **Enhance Interface**: Update `TranslationExecutionServices` to allow passing a `priority` ('CRITICAL' | 'BACKGROUND' | 'BATCH') to the `updateLibrary` function.
*   **Trigger Immediate Save**: Modify `executePageTranslation` to call `services.updateLibrary` with `CRITICAL` priority immediately after a page translation is successfully completed and stored in memory.

### 2. Update `TranslationJobRunner.ts` (Background Service)
*   **Support Priority Passing**: Update its `updateLibrary` implementation to accept and use the `priority` argument.
*   **Refactor `saveWithPriority`**:
    *   Ensure that when `CRITICAL` priority is used, it not only saves the new data immediately but also flushes any accumulated `pendingSaveData` (debounce buffer) to ensure complete data consistency.
    *   This fixes the issue where background jobs would wait 30 seconds before saving, exposing a large window for data loss.

### 3. Update `useAppTranslation.ts` (UI Hook)
*   **Pass-through Priority**: Update the `UseAppTranslationProps` interface to match the new `updateLibrary` signature, ensuring that the UI's translation loop also benefits from the immediate save capability provided by `useAppLibrary`.

### Verification
*   Verify that `TranslationExecutor` explicitly requests a `CRITICAL` save on page completion.
*   Verify that `TranslationJobRunner` correctly flushes pending data and executes an immediate save when requested.
