I will help you troubleshoot and fix the logging issue where only error logs are being generated.

The investigation reveals:
1.  **Default Behavior**: The app defaults to `info` level if `LOG_LEVEL` is not set. This *should* generate `app-YYYY-MM-DD.log` for normal logs and `error-YYYY-MM-DD.log` for errors.
2.  **Possible Cause**: It's likely that `LOG_LEVEL` is somehow set to `error` in your environment, or the `app-*.log` file is being ignored/overlooked.

I propose the following plan to force verbose logging and debug the configuration:

### 1. Force Debug Logging
I will create (or update) the `.env` file to explicitly set the log level to `debug`. This ensures that all logs (trace, debug, info, warn, error) are attempted to be written.

### 2. Add Diagnostic Output
I will modify `electron/logger.js` to print the active log configuration to the terminal immediately upon startup. This will tell us:
- What `LOG_LEVEL` the app actually sees.
- Where exactly the logs are being written (the full path).

### 3. Verification
After applying these changes, you will restart the app and we will check:
1.  The terminal output for the "Logger Configuration" message.
2.  The existence of `debug-YYYY-MM-DD.log` and `app-YYYY-MM-DD.log` in the log directory.
