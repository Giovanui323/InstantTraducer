I will modify `electron/main.js` to log the application version when the app starts.

1. **Edit** **`electron/main.js`**:

   * Locate the `app.on('ready', ...)` event handler.

   * Insert a log statement at the beginning of the handler to record the application version.

   * The log message will use `app.getVersion()` to retrieve the version from `package.json`.

**Example Change:**

```javascript
app.on('ready', async () => {
    logMain(`Application started. Version: ${app.getVersion()}`);
    // ... existing code ...
});
```

