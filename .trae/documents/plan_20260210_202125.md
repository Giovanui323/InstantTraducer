I will modify `src/App.tsx` to properly register ID redirects during file rename operations. This will prevent the system from blocking saves to the old ID (which leads to the 10-second timeout and potential state corruption) by immediately redirecting them to the new ID.

**Proposed Changes:**
1.  **Update `src/App.tsx`**:
    *   In `handleOpenProject`, inside the automatic metadata rename block: Add `library.registerRename(fileId, finalId)` after the successful rename.
    *   In the `RenameModal` `onRename` handler: Add `library.registerRename(oldId, finalId)` after the successful rename.

This ensures that the `updateLibrary` hook can catch saves targeting the old file ID and redirect them to the new one *before* hitting the "Transition Lock", thus avoiding the timeout and the "ghost state" issue.