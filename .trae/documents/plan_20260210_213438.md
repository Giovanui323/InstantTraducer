My plan is to fix the two critical inconsistencies identified:

1. **Fix** **`areSettingsEqual`** **in** **`SettingsModal.tsx`:**

   * Add `fastMode`, `proVerification`, and `customPrompt` to the comparison logic. This ensures the "Save" button is enabled when these settings are modified.

2. **Fix Model Overwrite Logic in** **`useAiSettings.ts`:**

   * Remove or modify the logic that forcefully overwrites the selected models based *only* on `fastMode` and `proVerification` flags upon settings load.

   * The flags should *influence* model selection (as it does in the modal UI), but the model saved in the settings should take precedence, preventing the user's manual choice from being overridden on every app reload.

