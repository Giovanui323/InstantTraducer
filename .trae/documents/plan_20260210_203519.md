I will address the issues in three specific tasks, with a strong emphasis on **high-quality, secure, and controllable logging** as requested.

### Task 4: Error Propagation & UX Feedback
**Goal:** Prevent silent failures and ensure user awareness of critical IO states.

1.  **UX Feedback for Blacklist:**
    *   Modify `src/hooks/useAppLibrary.ts` in `blockSave`.
    *   Add `toast.error` notification when a file is blacklisted, giving immediate feedback to the user.
2.  **IPC Error Handling & Queue Stop:**
    *   Modify `src/hooks/useAppLibrary.ts` to catch `renameProject` errors.
    *   Call `queueManager.pause()` on failure to stop processing on invalid IDs.
3.  **Async Rename Safety:**
    *   Modify `electron/projectHandlers.js` (`rename-translation`).
    *   Add `fs.access` check before rename.
    *   Improve error logging in the backend to include file path details (masked if needed) for easier debugging of "File not found" errors.

### Task 5: Metadata Lifecycle & Analytics
**Goal:** Fix statistics pollution and race conditions.

1.  **Reset Analytics:**
    *   Modify `src/App.tsx` to call `pdfRenderAnalytics.reset()` upon loading a new document.
2.  **Autodetect Race Condition:**
    *   Modify `src/App.tsx`.
    *   Pause/Block translation queue addition while `isAutodetecting` is true.
3.  **Asset Path Verification:**
    *   Verify and fix path generation in `electron/projectHandlers.js` for `save-original-pdf-buffer`.

### Task 6: Logging, Privacy & Debug Mode (Enhanced)
**Goal:** Implement "Good Logging" - Clean, Secure, and Useful.

1.  **Smart Privacy Logging (Gemini Provider):**
    *   Modify `src/services/geminiService.ts` / `src/services/aiService.ts`.
    *   **Redaction:** Never log the full System Prompt or Book Text in production. Replace with metadata: `[SYSTEM PROMPT] (Length: 4500 chars)`.
    *   **Debug Mode:** Only show full prompts if `verboseLogs` AND `process.env.NODE_ENV === 'development'` are both true.
    *   **API Keys:** Ensure robust masking (e.g., `sk-***1234`).
2.  **Strict Verbosity Control:**
    *   Modify `src/services/logger.ts`.
    *   Enforce log levels: `TRACE` logs must NOT appear unless `verboseLogs` is explicitly enabled.
    *   Structure logs to be readable but compact (avoid multi-line dumps for simple events).
3.  **Shutdown Handshake:**
    *   Modify `src/hooks/useAppLibrary.ts` (`flushSaves`).
    *   Log a clear summary of saved vs. blocked items during shutdown.
    *   Return a status to the main process indicating if the flush was clean or partial.