I will fix the issue where the new API Key is not used immediately and add the requested secure logging.

**Changes:**
1.  **Modify `src/hooks/useTranslationQueue.ts`**:
    *   Implement `processPageRef` and `pumpQueueRef` to ensure the translation queue always uses the latest settings (including the new API Key) instead of getting stuck with the old ones.

2.  **Modify `src/services/aiService.ts`**:
    *   Add a secure log in `translatePage` that shows the **provider** and the **last 4 characters** of the API Key being used (e.g., `Gemini Key: ...1234`).
    *   This will allow you to verify in the console/logs that the correct key is being passed, without exposing the full secret.

This addresses both the root cause of the error and your request for visibility.