I will resolve the "Orphan Assets" issue by enforcing consistency between the JSON save (which is correctly blocked by the blacklist) and the image save (which currently bypasses it).

The plan involves:

1.  **Expose Blocked Status**: Modify `src/hooks/useAppLibrary.ts` to expose a new `isBlocked(fileId)` function. This function will check if a given project ID is in the `blacklistedIdsRef` or `transitioningIdsRef` sets.
2.  **Enforce Check in UI**: Modify `src/App.tsx` to check `library.isBlocked(...)` inside the `saveSourceForPage` callback. If the project is blocked, the image save will be aborted, preventing the creation of orphaned `.jpg` files in the assets folder.

This ensures that if the system refuses to update the project index (JSON), it will also refuse to write the associated image files, maintaining file system integrity.
