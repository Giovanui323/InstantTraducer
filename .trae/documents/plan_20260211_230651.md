I will address the three reported issues (Hallucinations, Race Conditions, Infinite Loops) and the persistence of errors in the logs by reinforcing the implementation and adding robustness fixes.

### 1. Fix "Page 150" Hallucination (Hallucination)
**File:** `src/services/verifierPrompts.ts`
- **Action:** Ensure the "IGNORE BIBLIOGRAPHIC REFERENCES" rule is explicitly and strongly defined in the system prompts.
- **Goal:** Prevent the AI from flagging bibliographic citations (e.g., "see p. 150") as missing text.

### 2. Fix Race Condition & Data Contamination (Race Condition)
**File:** `src/hooks/useAppLibrary.ts`
- **Action:** Verify the critical fix that blocks saves during project switching.
- **Enhancement:** I will also refine the `[RECOVERY]` logic to avoid false alarms when uploading new files, ensuring `recentBooks` is synchronized before the session is marked active.

### 3. Break Infinite Retry Loop & Fix Queue Stalling (Infinite Loop)
**Files:** `src/services/translation/TranslationExecutor.ts`, `src/hooks/useTranslationQueue.ts`
- **Action:** Ensure `TranslationExecutor` forces a save after maximum retries instead of discarding the result.
- **Enhancement:** I will modify `useTranslationQueue.ts` to add better diagnostics to the "STALLED" warning. The logs show "STALLED" even when pages should be active; I will add logging for `activePages` count to debug this phantom stall and ensure the queue recovers automatically.

### 4. Improve Rename Reliability (Infrastructure)
**File:** `electron/projectHandlers.js`
- **Action:** Increase the file existence check timeout in the `rename-translation` handler from 2 seconds (20x100ms) to 5 seconds (50x100ms).
- **Reason:** The frontend blocks saves for 8 seconds, but the backend only waits 2 seconds for the file to appear. This mismatch likely causes the "File originale non trovato" error seen in the logs. Increasing this timeout will ensure the file system has settled before the rename is attempted.