I will modify `src/hooks/useAppLibrary.ts` to implement a content equality check and reduce logging verbosity.

**1. Prevent Redundant Saves in `updateLibrary`**
*   Currently, `updateLibrary` queues a save and updates the `timestamp` every time it is called, even if the data hasn't changed.
*   I will add a deep equality check using `fast-deep-equal` (already imported as `equal`) to compare the incoming `data` with the existing state in `recentBooksRef`.
*   The check will ignore the `timestamp` field, ensuring that we only proceed if there are actual content changes (e.g., translations, page number, metadata).
*   If no content changes are detected, the function will return early, preventing the state update, save queue addition, and subsequent disk write.

**2. Reduce Logging Noise in `processSaveQueue`**
*   I will modify the logging in `processSaveQueue` to be less verbose.
*   The "Processing save queue" log will be changed from `log.step` (which sends IPC messages) to `console.debug` or `log.info` to avoid spamming the console and main process, or I will add a check to only log if `uniqueRequests.length > 0`.
*   This addresses the user's complaint about "excessive logs".

**Verification**
*   Since I cannot run the app, I will rely on the logic that preventing identical updates stops the infinite loop of saves reported by the user (20,000+ queued requests).
*   The code references provided (`src/hooks/useAppLibrary.ts`) confirm the location of the issue.