I will optimize the pipeline to "Fail Fast" during the quality verification phase when API quotas are exhausted, preventing useless waiting time.

### Implementation Plan

1.  **Modify `src/services/geminiService.ts`**:
    *   Update `verifyTranslationQualityWithGemini` to implement a "Short-Circuit" check.
    *   Before invoking the AI, it will check if the target model (and its fallback chain) is currently in a `cooldown` state (due to previous 429/Quota errors).
    *   If all usable models are in cooldown, it will immediately throw a `Quota exceeded (Fast Fail)` error, bypassing the API call entirely.
    *   This eliminates the ~45s (or 90s) wait time when the system already knows the request will fail.

2.  **Verify `src/hooks/useAppQuality.ts`**:
    *   Confirm that the existing error handler correctly intercepts this new "Fast Fail" error.
    *   The current logic (`msg.includes('quota')`) will classify this as a `RATE_LIMIT` issue.
    *   **Result**: The verification will be skipped (marked as "Verified with Warning") instead of blocking the pipeline or failing the page, preserving the translation.

This approach directly addresses the inconsistency where the pipeline "waits uselessly" despite knowing the API is unavailable.