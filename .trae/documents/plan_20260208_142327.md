# Analisi e Proposta di Fix per ID Mismatch (Race Condition)

Ho esaminato le tue recenti modifiche in [useAppLibrary.ts](file:///Users/lucasicignano/Lavoro%20temporaneo/UNIVERSITA/FIVER/gemini-pdf-translator-di-libri-2/src/hooks/useAppLibrary.ts) e [App.tsx](file:///Users/lucasicignano/Lavoro%20temporaneo/UNIVERSITA/FIVER/gemini-pdf-translator-di-libri-2/src/App.tsx). Il "CRITICAL FIX" che hai aggiunto per la risoluzione dell'ID è un ottimo passo avanti, ma non risolve ancora completamente la **Race Condition** segnalata nei log del 07/02.

### Cosa manca ancora:
1.  **Il Backend non rifiuta**: Nonostante il log `WARN ID Mismatch`, il backend in `main.js` procede comunque a scrivere sul file, il che può corrompere i dati o creare duplicati.
2.  **Mancata sincronizzazione**: `useAppLibrary` e `useProjectManagement` hanno due `fileIdCacheRef` separate. Se uno rinomina un file, l'altro potrebbe non saperlo subito.
3.  **Mancanza di Ack**: Se il Backend rileva che l'ID deve cambiare (es. ricalcolo hash), il Frontend non ha una logica per ricevere il nuovo ID e aggiornarsi "al volo" durante il salvataggio.

### Piano di Intervento:

## 1. Backend: Rifiuto Categorico (Main Process)
*   Modificherò **[main.js](file:///Users/lucasicignano/Lavoro%20temporaneo/UNIVERSITA/FIVER/gemini-pdf-translator-di-libri-2/electron/main.js)** affinché, in caso di mismatch critico, restituisca `{ success: false, error: 'ID_MISMATCH', expectedId }`. Questo impedisce salvataggi incrociati.

## 2. Frontend: Riallineamento Dinamico (useAppLibrary)
*   Aggiornerò `updateLibrary` per gestire la risposta `ID_MISMATCH`. Se il backend rifiuta, il frontend aggiornerà i propri stati interni (`recentBooks`, `fileIdCacheRef`, `currentProjectFileId`) con il nuovo `expectedId` e riproverà immediatamente il salvataggio.

## 3. Unificazione Cache ID (App.tsx)
*   Modificherò **[App.tsx](file:///Users/lucasicignano/Lavoro%20temporaneo/UNIVERSITA/FIVER/gemini-pdf-translator-di-libri-2/src/App.tsx)** per passare la `fileIdCacheRef` di `useAppLibrary` direttamente a `useProjectManagement`. In questo modo, entrambi i hook condivideranno la stessa "verità" sugli ID dei file.

## 4. Rafforzamento Rinomina (useProjectManagement)
*   Assicurerò che dopo ogni operazione di `rename-translation`, la cache condivisa venga aggiornata istantaneamente per evitare che salvataggi pendenti usino il vecchio ID.

Questo approccio chiude il cerchio: il Backend protegge il disco e il Frontend impara a correggersi se sta usando un ID obsoleto. Procedo?