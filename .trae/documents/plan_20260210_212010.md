I will modify `src/App.tsx` to add a check at the beginning of the `handleOpenProject` function.

**Specific changes:**
1.  In `src/App.tsx`, inside `handleOpenProject`:
    -   Add a condition to check if `library.currentProjectFileId` is equal to the requested `fileId`.
    -   If they match, log an info message and return immediately to prevent the project reload sequence (reset, cleanup, abort).

```typescript
    // Prevent reloading the same project if it is already active
    if (library.currentProjectFileId === fileId) {
      log.info(`[App] Progetto ${fileId} già attivo. Ignoro ricaricamento.`);
      return;
    }
```

This ensures that redundant "Switching to..." events do not trigger a reset of the state, preserving ongoing operations like translations.