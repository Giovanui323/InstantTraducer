I will improve the logging visibility and detail to ensure that critical sequences like "Save Cancellation -> Rename Failure" are clearly traceable in the future.

The user noted that the logs were missing this context. This is because:
1.  The `LogViewer` treats `[STEP]` messages as plain text (gray), making them hard to spot.
2.  The `cancelPendingSaves` function didn't log *why* it was cancelling a save.

### Plan:

1.  **Enhance `src/components/LogViewer.tsx`**:
    *   Update the filtering logic to explicitly recognize `[STEP]` tags.
    *   Update the color coding (`getLineColor`) to highlight `[STEP]` messages (e.g., in Indigo/Purple) so operational steps stand out from general info.

2.  **Improve `src/hooks/useAppLibrary.ts`**:
    *   Update `cancelPendingSaves` to accept an optional `reason` parameter.
    *   Log the specific reason for cancellation (e.g., "Reason: Blocked for Rename").
    *   Update `blockSave` to accept an optional `reason` and pass it to `cancelPendingSaves`.

3.  **Update Call Sites in `src/App.tsx`**:
    *   Update `library.blockSave` calls to include the reason "Rename Operation" for better traceability.

This will make the logs "speak" more clearly about cause-and-effect relationships.
