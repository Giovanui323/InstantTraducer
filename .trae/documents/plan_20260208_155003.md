I will resolve the user's confusion by improving the feedback provided by the "Clean Old Logs" function. Currently, it simply says "No old log files found" if no files are deleted, which is confusing when the user sees files (which are likely recent and thus preserved).

1.  **Modify `electron/logHandlers.js`**:
    *   Update the `cleanup-old-logs` IPC handler to return an object `{ deletedCount, totalFound, daysToKeep }` instead of just a number.
    *   This allows the frontend to distinguish between "no logs exist" and "logs exist but are too recent".

2.  **Modify `src/components/SettingsModal.tsx`**:
    *   Update the button's `onClick` handler to interpret the new response.
    *   If `deletedCount > 0`, show the success message.
    *   If `deletedCount === 0` but `totalFound > 0`, show a message explaining that files exist but are recent (e.g., "Found X log files, but none are older than 7 days").
    *   If `totalFound === 0`, show "No log files found".

This approach clarifies the system's behavior without removing the safety protection for recent logs.
