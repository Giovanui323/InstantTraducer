I have identified the cause of the timeout errors. The current verification timeout is set to **120 seconds (2 minutes)**, which appears to be insufficient for the `gemini-3-flash-preview` model on some pages, causing it to fail repeatedly.

Here is the plan to resolve this issue and make the verification process more robust:

### 1. Increase Verification Timeout
Increase the `AI_VERIFICATION_TIMEOUT_MS` constant in `src/constants.ts` from **120,000ms (2 minutes)** to **240,000ms (4 minutes)**. This will give the model ample time to respond even during periods of high latency or complex page analysis.

### 2. Implement Fallback for Timeouts
Update `src/services/geminiService.ts` to handle timeout errors in the verification step (`verifyTranslationQualityWithGemini`). Currently, it only falls back to a different model on "Quota" errors. I will modify it to also trigger the fallback mechanism (switching to `gemini-2.5-flash`) when a **Timeout** occurs, ensuring that a single slow model doesn't block the entire process.

This two-pronged approach (longer timeout + smart fallback) should eliminate the "Verifica qualità fallita" errors you are seeing.