I have identified the cause of the redundant rendering and logging. The `ensureImageLoaded` function in `App.tsx` is being triggered multiple times concurrently for the same page due to rapid state updates or effect re-runs. Since it lacks a mechanism to track in-flight requests, it launches multiple parallel rendering tasks for the same page, each succeeding and logging a success message.

To fix this, I will:

1. Add a `loadingImagesRef` (using `useRef`) to the `App` component to track which pages are currently being processed.
2. Modify the `ensureImageLoaded` function to check this ref before starting a new render. If a render for the page is already in progress, it will skip the request (debouncing/deduplication).
3. Ensure the ref is correctly updated (added on start, removed on completion/failure).

This will prevent the "8 renders in 1 second" issue and significantly reduce CPU/memory usage.

**Files to modify:**

* `src/App.tsx`

**Implementation Steps:**

1. Initialize `loadingImagesRef` in `App.tsx`.
2. Update `ensureImageLoaded` logic to use `loadingImagesRef` for concurrency control.

