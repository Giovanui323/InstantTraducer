I have identified the issue. The "fake" unblock occurs because the frontend correctly clears its local blocked state after the timeout, but **fails to notify the backend** to do the same.

The `blockSave` function in `src/hooks/useAppLibrary.ts` sets a timeout to auto-unblock the ID. Inside this timeout, it removes the ID from `transitioningIdsRef` and `blacklistedIdsRef` (frontend state), but it **misses the call** to `window.electronAPI.unblockSave(fileId)`. Consequently, the backend's `WriteSequencer` remains unaware that the block has been lifted and continues to reject save attempts with `BLOCKED_BY_BACKEND`.

### Plan to Fix
I will modify `src/hooks/useAppLibrary.ts` to ensure the backend is notified when the auto-unblock timeout triggers.

1.  **Edit `src/hooks/useAppLibrary.ts`**:
    *   Locate the `blockSave` function.
    *   Inside the `setTimeout` callback, within the `if (changed)` block:
    *   Add the missing call: `window.electronAPI?.unblockSave(fileId).catch(...)`.

This will ensure that when the frontend logs "Auto-unblocked...", it also instructs the backend to lift the block, resolving the `BLOCKED_BY_BACKEND` error for subsequent saves.