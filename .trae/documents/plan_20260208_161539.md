I will implement a self-healing mechanism for the `groups.json` configuration file to handle critical validation errors automatically.

**Changes to `electron/projectHandlers.js`:**
1.  **Enhance `load-groups` handler**:
    -   Currently, if the groups file is corrupted and recovery fails, the app returns an empty list but leaves the corrupted file on disk, causing the error to persist on every launch.
    -   I will add logic to **automatically backup** the corrupted `groups.json` file (e.g., to `groups.json.corrupted_<timestamp>`).
    -   I will then **reset** the live `groups.json` file to a valid empty array `[]`.
    -   This ensures the application starts cleanly on the next launch and stops reporting the "Validation failed" error, while preserving the original data in a backup file for potential manual inspection.

This approach resolves the "Critical Error" by isolating the bad data and restoring a valid state.