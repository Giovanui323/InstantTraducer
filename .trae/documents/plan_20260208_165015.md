Il problema è quasi certamente causato dalla mancanza della **API Key** configurata nelle impostazioni.

Quando clicchi su "Play", il sistema toglie la pausa (`isPaused = false`), e l'interfaccia mostra "Traduzione automatica in avvio". Tuttavia, il processo di traduzione non parte effettivamente perché c'è un controllo di sicurezza che blocca tutto se le API non sono configurate correttamente. Attualmente, l'interfaccia non ti avvisa di questo blocco in quella specifica schermata, lasciandoti in attesa indefinita.

Ecco il piano per risolvere il problema e migliorare l'interfaccia per il futuro:

1. **Modificare** **`ReaderView.tsx`**:

   * Aggiungere le proprietà `isApiConfigured` e `onOpenSettings` alle `ReaderViewProps`.

   * Modificare la logica di visualizzazione del messaggio "Traduzione automatica in avvio". Se le

