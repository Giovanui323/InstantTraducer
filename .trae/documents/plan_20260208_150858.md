I confirmed that the problem is NOT solved yet. While the user implemented a "CRITICAL FIX" in the frontend to check IDs before sending, the system is still vulnerable because:
1.  **Backend**: `main.js` warns about mismatches but *still writes the file*, causing corruption.
2.  **Synchronization**: `useProjectManagement` uses a separate, empty `fileIdCacheRef` instead of sharing the one from `useAppLibrary`.
3.  **Error Handling**: The frontend does not listen for or handle backend rejections to auto-correct the ID.

My plan remains the same, focused on closing these gaps.

### 1. Backend: Enforce ID Integrity (`electron/main.js`)
*   **Goal**: Prevent data corruption by rejecting saves when the File ID does not match the content's expected ID.
*   **Change**: In the `save-translation` handler, locate the "ID Mismatch critico" check.
*   **Action**: Instead of just logging `logWarn`, explicitly return `{ success: false, error: 'ID_MISMATCH', expectedId }` and abort the write operation.

### 2. Frontend: Handle Rejection & Auto-Correct (`src/hooks/useAppLibrary.ts`)
*   **Goal**: Make the frontend resilient to `ID_MISMATCH` errors.
*   **Change**: Update the `updateLibrary` function.
*   **Logic**:
    1.  Check if `saveTranslation` returns `error: 'ID_MISMATCH'`.
    2.  If yes, capture the `expectedId` returned by the backend.
    3.  **Immediate State Correction**:
        *   Update `fileIdCacheRef.current` mapping.
        *   Update `recentBooks` entry to use the new ID.
        *   If the mismatched file is the *current* project, update `currentProjectFileId`.
    4.  **Retry**: Automatically re-trigger the save operation with the correct `fileId`.

### 3. Unified State Management (`src/App.tsx` & `src/hooks/useProjectManagement.ts`)
*   **Goal**: Ensure `useAppLibrary` (Library Logic) and `useProjectManagement` (Rename/Maintenance Logic) share the exact same source of truth for File IDs.
*   **Change in `App.tsx`**: Pass `library.fileIdCacheRef` as a prop to `useProjectManagement`.
*   **Change in `useProjectManagement.ts`**:
    *   Remove the internal `const fileIdCacheRef = useRef({})`.
    *   Accept `fileIdCacheRef` from props.
    *   Update all references (e.g., in `redoAllPages`) to use the shared ref.

### 4. Robust Rename Handling (`src/hooks/useProjectManagement.ts`)
*   **Goal**: Prevent "Ghost Writes" immediately after a rename.
*   **Change**: In `scanAndRenameOldFiles` and `scanAndRenameAllFiles`, ensure that successful renames update the **shared** `fileIdCacheRef` immediately.
