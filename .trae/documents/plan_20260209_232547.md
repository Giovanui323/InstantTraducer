I will modify `src/services/geminiService.ts` to implement a "Smart Failover" for Zero Quota scenarios.

**The Strategy:**

1. **Identify the "Hard Limit"**: I will create a helper `isHardLimitZero(error)` to detect the specific `limit: 0` error.
2. **Kill the Retry Loop**: I will update the `retry()` utility usage in `translateWithGemini`. Currently, it blindly retries twice on errors. I will modify the condition to **abort retries immediately** if a Hard Limit is detected, preventing "Attempt 1, Attempt 2" on a dead model.
3. **Permanent Model Ban**: Instead of a short cooldown, if a Hard Limit is detected for a model (e.g., `Gemini 1.5 Pro`), I will set an extremely long cooldown (24 hours) for *that specific model*. This effectively "stops total requests" for that model as requested, while allowing the system to seamlessly switch to the next available model (e.g., `Flash`) which might still have quota.

**File to be modified:**

* `src/services/geminiService.ts`

**Changes:**

* Add `isHardLimitZero` function.

* Update `retry` callback in `translateWithGemini`.

* Update `retry` callback in `extractPdfMetadataWithGemini`.

* Update cooldown logic in `translateWithGemini` and `verifyTranslationQualityWithGemini` to apply a "long ban" instead of "short cooldown" for hard limits.

