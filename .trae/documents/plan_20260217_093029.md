I will modify the `SettingsModal` component to provide immediate visual feedback when the user clicks "Save Configuration".

The changes will involve:

1. **Update** **`SettingsModalProps`**: Change the `onSave` return type to `Promise<void> | void` to correctly handle asynchronous save operations.
2. **Add Loading State**: Introduce a local `isSaving` state variable in `SettingsModal`.
3. **Update Save Handler**: Modify the "Save Configuration" button's `onClick` handler to:

   * Set `isSaving` to `true`.

   * `await` the `onSave` operation.

   * Handle potential errors (though the hook handles most).
4. **Update UI**:

   * Disable the "Save Configuration" button while `isSaving` is true.

   * Change the button text to "Salvataggio..." and show a spinner (using `Loader2` or `Activity` icon) during the save process.

This ensures the user knows the application is processing their request and prevents multiple clicks/submissions while the save is in progress.

**File to modify:**

* `src/components/SettingsModal.tsx`

