I will implement changes to prevent accidental double-submission of translation and verification tasks, which causes the "abort previous" logs and resource waste.

### 1. Prevent Double Execution in Hooks
I will modify the core hooks to ignore requests if an operation is already in progress for the target page.

*   **`src/hooks/useAppTranslation.ts`**:
    *   In `retranslatePage`: Add a check for `pageStatusRef.current[page]?.loading || pageStatusRef.current[page]?.processing`. If true, log a warning and return early.

*   **`src/hooks/useAppQuality.ts`**:
    *   In `verifySingleTranslatedPage`: Add a check for `verificationMapRef.current[page]?.state === 'verifying'`. If true, log a warning and return early.

### 2. Enhance Visual Feedback in UI
I will update the UI to visually indicate that the action is disabled when an operation is running.

*   **`src/components/ReaderView.tsx`**:
    *   Find the "Riprova" (Retry) button in the loading/processing overlay.
    *   Add the `disabled` attribute linked to the page status (`pageStatus[p]?.loading || pageStatus[p]?.processing`).
    *   Apply visual styles (opacity, cursor) to reflect the disabled state.

### Verification
*   I will verify that clicking "Retry" multiple times rapidly only triggers one request (one log entry).
*   I will verify that the "Retry" button appears disabled while the spinner is active.
