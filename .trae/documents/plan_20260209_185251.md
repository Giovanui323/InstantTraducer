I will implement the following changes to add **Blue** and **Red** highlight colors, using a new utility file to keep the code clean as requested:

1.  **Create `src/utils/highlightStyles.ts`**:
    *   This new file will centralize highlight logic.
    *   It will define the supported colors (Yellow, Green, Blue, Red).
    *   It will provide helper functions:
        *   `getHighlightClasses(color, theme)`: Returns the correct Tailwind classes for a given color and theme.
        *   `getHighlightCursor(color)`: Returns the SVG cursor string for the highlighter tool.
        *   `HIGHLIGHT_COLORS`: A constant array of color definitions (id, label, hex value) to easily generate the UI.

2.  **Update `src/utils/readerStyling.ts`**:
    *   Update the `ReaderTheme` interface to include `blue` and `red` in the `highlight` object.
    *   Update the `READER_THEMES` (light, sepia, dark) to define the specific RGBA values for Blue and Red.

3.  **Update `src/components/ReaderView.tsx`**:
    *   Import the new helpers from `highlightStyles.ts`.
    *   Replace the hardcoded color buttons with a loop over `HIGHLIGHT_COLORS`.
    *   Update the main highlighter button to dynamically show the active color.

4.  **Update `src/components/MarkdownText.tsx`**:
    *   Import `getHighlightClasses` from `highlightStyles.ts`.
    *   Refactor the highlight rendering logic to use this helper function instead of the current hardcoded conditional checks.

This approach adds the new colors while refactoring the code to be more maintainable and cleaner.