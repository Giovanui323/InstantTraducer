I will modify the `performDeepHealthCheck` function in `electron/fileUtils.js` to correctly detect missing original PDF files.

**Changes to `electron/fileUtils.js`**:
1.  **Read Project JSON**: Inside the project scanning loop in `performDeepHealthCheck`, I will add logic to read and parse the content of each project's JSON file.
2.  **Verify `originalFilePath`**: I will extract the `originalFilePath` property from the parsed JSON.
3.  **Check File Existence**: I will use `fs.promises.access` to verify if the file at `originalFilePath` exists on the disk.
4.  **Update Error Tracking**:
    *   If the file is missing, I will increment `inconsistencyTracker.missingAssets`.
    *   I will log a specific warning: `MISSING ORIGINAL FILE: Project '${projFile}' references missing file '${originalFilePath}'`.

**Why this fixes the issue**:
The current implementation only checks if the *asset folder* (containing images/cache) exists, but ignores the *original PDF file* path stored in the project data. This change ensures the health check validates the integrity of the source file as well.
