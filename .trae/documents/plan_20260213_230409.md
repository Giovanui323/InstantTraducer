I will address the critical persistence bug and data loss issue by implementing a robust fix in the backend's `WriteSequencer` and `rename-translation` logic, and adding a safety mechanism in the frontend.

### 1. Backend: Fix Deadlock in Rename Operation
**File:** `electron/projectHandlers.js`
- **Action:** Wrap the `rename-translation` logic in a `try...finally` block.
- **Logic:** Ensure `writeSequencer.unblock(oldFileId)` is **always** called in the `finally` block, regardless of whether the rename succeeds, fails, or times out. This prevents the "Ghost Lock" where the ID remains blocked indefinitely.
- **Safety:** Verify if `newFileId` needs unblocking as well.

### 2. Backend: Enhance Rollback Logic
**File:** `electron/projectLogic.js`
- **Action:** Review and strengthen `performRename` to ensure atomic operations.
- **Logic:** If the JSON write fails after assets are moved, explicitly rollback the asset move to restore the previous state.

### 3. Frontend: Implement Circuit Breaker for Persistence
**File:** `src/hooks/useAppLibrary.ts`
- **Action:** Modify `processSaveQueue` to track consecutive `BLOCKED_BY_BACKEND` errors.
- **Logic:**
    - If `BLOCKED_BY_BACKEND` occurs more than 5 times consecutively, **pause the save queue**.
    - Trigger a user alert (toast/notification) informing them that saving is blocked and they should restart or check the file status.
    - Prevent the queue from infinitely retrying and filling up with failing requests.

### 4. Verification
- **Test:** Simulate a rename failure (e.g., by locking the file or mocking a failure) and verify that `save-translation` requests for the old ID are no longer rejected with `BLOCKED_BY_BACKEND`.
- **Test:** Verify that the frontend correctly pauses the queue and notifies the user if the backend reports a block.
