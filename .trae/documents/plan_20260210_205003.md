I will modify the codebase to ensure that the save blocking mechanism allows for immediate unblocking and clearing of timeouts, specifically addressing the issue where renamed files remain blocked until the safety timeout expires.

### 1. Enhance `useAppLibrary.ts`
I will modify `src/hooks/useAppLibrary.ts` to manage the timeouts for `blockSave`.
-   Add a `blockSaveTimeoutsRef` to store active timeout IDs.
-   Update `blockSave` to store the timeout ID when a block is initiated.
-   Update `unblockSave` to check for and `clearTimeout` the pending safety timer for the given `fileId`, preventing the "Auto-unblocked" log and unnecessary state retention.

### 2. Update Rename Logic in `App.tsx`
I will update the rename handling logic in `src/App.tsx` to explicitly unblock the *original* file ID (which was the one blocked) instead of just the new ID.
-   In `handleAutoRename` (inside `continueUploadWithLanguage`): Call `library.unblockSave(fileId)` (the old ID) after success.
-   In `RenameModal` callback: Call `library.unblockSave(oldId)` after success.

### 3. Verification
I will verify the changes by:
-   Checking that `blockSave` correctly stores timeouts.
-   Checking that `unblockSave` clears them.
-   Ensuring the rename flow unblocks the correct ID immediately.
