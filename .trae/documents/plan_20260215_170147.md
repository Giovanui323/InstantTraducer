I will fix the issue where the interface appears stuck during verification and ensure active verifications are visible in the queue.

1. **Expose Active Verifications**:

   * Modify `src/hooks/useVerificationQueue.ts` to expose `activePagesRef`.

   * Modify `src/hooks/useAppQuality.ts` to expose \`

