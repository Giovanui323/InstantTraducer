I will fix the identified issues with the following steps:

1. **Reduce Verification Timeout**:

   * Modify `src/constants.ts` to reduce `AI_VERIFICATION_TIMEOUT_MS` from 5 minutes (300,000ms) to **60 seconds (60,000ms)**. This ensures that stuck verifications fail quickly and don't block the queue.

2. **Fix IPC Flooding (High Frequency Saves)**:

   * Modify `src/services/translation/TranslationJobRunner.ts`.

   * The `saveWithPriority` method currently ignores the `priority` argument and saves immediately via `window.electronAPI.saveTranslation`.

   * I will implement a **debounce mechanism** (3 seconds) for `BACKGROUND` and `BATCH` priorities within `TranslationJobRunner`, similar to how `useAppLibrary` handles it. This will drastically reduce IPC calls during background jobs.

3. **Fix Translation Quality (French Residuals)**:

   * Modify `src/services/geminiService.ts`.

   * Update the `translateWithGemini` function to strengthen the retry prompt.

   * Change the instruction from "NON OMETTERE NULLA" (which might encourage keeping original text) to **"TRADUCI TUTTO IN ITALIANO. Non lasciare testo in lingua originale."** (Translate everything to Italian. Do not leave text in original language).

4. **Verification**:

   * I will verify the changes by checking the code (static analysis) and ensuring the logic handles the debounce correctly.

