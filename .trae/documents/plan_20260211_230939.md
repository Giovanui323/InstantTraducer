I will modify `src/components/ReaderView.tsx` to arrange the buttons in the "Dubbi & verifica" modal vertically.

1. **Locate the Button Container**: In `src/components/ReaderView.tsx`, inside the `NotesModal` component, I will find the `div` that contains the action buttons (Reanalyze, Verify, Fix, Force Pro) around line 398.
2. **Update CSS Classes**: I will change the class `flex items-center gap-2` to `flex flex-col items-start gap-2`. This will stack the buttons vertically instead of horizontally.

   * `flex-col`: Stacks items vertically.

   * `items-start`: Aligns items to the left (preventing the small circular "Reanalyze" button from stretching).

