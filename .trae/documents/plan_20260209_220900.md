I have identified the cause of the `[CRITICAL] Blocked cross-save` error. It occurs in `src/hooks/useAppLibrary.ts` when the application attempts to save a file while the `activeSessionId` is set, but the corresponding book object is missing from the `recentBooks` memory cache (likely due to a race condition during library refresh or context switch). This results in `activeBook` being `undefined`, which triggers the safety block intended to prevent cross-project overwrites.

To fix this, I will implement a "Recovery Mode" in the `updateLibrary` function. Instead of blocking the save when the active book is missing from memory, we will assume the active session ID is correct and allow the save to proceed, thereby restoring the book to the library and preventing data loss.

**Plan:**
1.  **Modify `src/hooks/useAppLibrary.ts`**:
    *   Locate the `updateLibrary` function.
    *   Update the cross-save protection logic (around line 326).
    *   Add a specific check: if `activeBook` is undefined but `currentProjectFileIdRef.current` is set, log a warning (instead of an error) and **allow the save** using the current session ID.
    *   This ensures that if the UI loses track of the book object in memory but maintains the session ID, the save operation will "heal" the state rather than failing.

This change directly addresses the user's report of "losing reference to current file during long operations" by making the save logic robust against temporary state inconsistencies.