I will modify the "Safety Abort" mechanism in `src/App.tsx` to ensure that a critical failure on a page only interrupts the translation of the **immediately following page**, rather than all subsequent pages.

### Proposed Changes

#### `src/App.tsx`
- **Update `onSevereError` function**:
  - Change the condition `if (p > failedPage)` to `if (p === failedPage + 1)`.
  - This ensures that if Page 33 fails, only Page 34 will be interrupted. Page 38 or 39, if running in parallel, will continue undisturbed unless their own immediate predecessor fails.

```typescript
// Current
if (p > failedPage) { ... }

// New
if (p === failedPage + 1) { ... }
```

This change directly addresses the issue where a failure on Page 33 incorrectly causes an abort on Page 38/39.