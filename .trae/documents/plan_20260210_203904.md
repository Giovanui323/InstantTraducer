I will address the file locking and page count issues, adding detailed logging as requested to monitor the fixes.

### 1. Fix File Locking & Buffer Contention (with Logs)
I will ensure PDF file writes are safely queued and logged to prevent race conditions and performance degradation.

*   **File:** `electron/projectHandlers.js`
*   **Action:**
    *   Wrap `save-original-pdf-buffer` logic inside `writeSequencer.enqueue(fileId, ...)`.
    *   Wrap `copy-original-pdf` logic inside `writeSequencer.enqueue(fileId, ...)`.
    *   **Add Logging:**
        *   Log when the sequencer *starts* processing the PDF write request (proving it entered the queue).
        *   Log when the write operation *completes* (proving it finished safely).

### 2. Fix Page Count Discrepancy (with Logs)
I will implement a "self-healing" check that corrects the JSON metadata using the authoritative PDF page count.

*   **File:** `src/App.tsx`
*   **Action:**
    *   In `handleOpenProject`, after `pdfjsLib` loads the document:
        *   Compare `pdf.numPages` (actual) vs `data.totalPages` (stored).
        *   **Add Logging:** If they differ, log a clear warning: `"Discrepanza pagine rilevata: JSON=${data.totalPages} vs PDF=${pdf.numPages}. Aggiornamento in corso..."`.
        *   Call `library.updateLibrary` to persist the correct `totalPages`.

### 3. Verification
*   Review the code to ensure `writeSequencer` is correctly applied.
*   Check that the new logs are in place to trace the execution flow.
