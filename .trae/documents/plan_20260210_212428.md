I agree with your observation. Different models (like Flash vs Pro) often require different prompting strategies to achieve optimal results. Currently, the system uses the same prompt for all models.

I will proceed with the following plan:

1.  **Fix Model Selection Bug**:
    *   Modify `src/services/aiService.ts` to correctly use the selected model (e.g., Flash when "Modalità Veloce" is active) instead of hardcoding `gemini-3-pro-preview`.

2.  **Enable Model-Specific Prompts**:
    *   Update `getTranslateSystemPrompt` in `src/services/prompts.ts` to accept the `model` as a parameter.
    *   Update `src/services/geminiService.ts` to pass the active `model` when generating the system prompt.

3.  **Future-Proofing**:
    *   This refactoring will allow us to easily implement different prompt logic for Flash (e.g., shorter, more direct instructions) vs Pro (e.g., more nuanced instructions) in the next step, based on your specific requirements.

Do you want me to also implement a specific variation for the Flash prompt now (e.g. simplifying it), or just prepare the structure?