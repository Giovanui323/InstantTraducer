I will optimize the `LogViewer` component to address the "flashing" issues and add the requested log filtering features.

### 1. Performance Optimization (Fix "Flashes")
- **Issue**: Rendering thousands of log lines at once causes browser repaints/flashes.
- **Solution**:
  - Implement **pagination/virtualization**: Initially render only the first 1000 lines.
  - Add a **"Load More" button** at the bottom to load more lines as needed.
  - Use `useMemo` to cache the processed log lines, preventing expensive re-calculations on every render.

### 2. Log Filtering Feature
- **Add Filter Controls**: Add checkboxes in the header to toggle visibility of different log levels:
  - ✅ **Error** (Red)
  - ✅ **Warning** (Orange)
  - ✅ **Info** (Blue)
  - ✅ **Debug** (Gray)
- **Filtering Logic**: Filter the log lines based on the selected categories before rendering.

### Implementation Details
- **File**: `src/components/LogViewer.tsx`
- **State Changes**:
  - Add `filters` state for the 4 log levels.
  - Add `visibleLines` state to track how many lines are currently shown.
- **UI Changes**:
  - Insert a filter bar below the header.
  - Display "Showing X of Y lines" status.
