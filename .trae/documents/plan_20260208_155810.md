I will implement the requested logging changes to reduce log volume and improve organization.

### 1. Modify `electron/logger.js`
- **Disable Debug by Default**: Change the default log level from `debug` to `info` even in development mode.
- **Split Log Files**: Implement logic to route logs to different files based on severity:
  - `error` level -> `error-YYYY-MM-DD.log`
  - `debug` and `trace` levels -> `debug-YYYY-MM-DD.log`
  - `info` and `warn` levels -> `app-YYYY-MM-DD.log`

### 2. Update `electron/logHandlers.js`
- **Update File Listing**: Modify `list-log-files` to include `error-*.log` and `debug-*.log` files (currently only lists `app-*.log`).
- **Update Cleanup**: Modify `cleanup-old-logs` to identify and delete old files for all three log types.

This ensures that under normal operation, no huge debug logs are created. When debug is enabled (via environment variable), it will be isolated in its own file, preventing it from cluttering the main application log.