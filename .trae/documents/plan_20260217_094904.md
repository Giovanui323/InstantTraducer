I will modify the `useSearch` hook to include the file name in the search results list, as requested.

### **Implementation Plan**

1.  **Modify `src/hooks/useSearch.ts`**:
    *   Update the `title` property in the `searchResults` construction.
    *   Change `title: \`Pagina \${p}\`` to `title: \`Pagina \${p} \${documentName ? \`- \${documentName}\` : ''}\``.

This change will make the search results display "Pagina X - [FileName]" instead of just "Pagina X", making it clear which file the result belongs to.