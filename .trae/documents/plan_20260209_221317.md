I will implement a robust debounce mechanism to reduce the frequency of saving translation files to disk, addressing the "redundant save loop" and "I/O Stress" issue.

### **Plan:**

1.  **Update `src/hooks/useAppLibrary.ts`**:
    *   Locate the `updateLibrary` function.
    *   Change the debounce delay for `BACKGROUND` priority from `3000` ms (3 seconds) to `30000` ms (30 seconds).
    *   This ensures that rapid updates (e.g., from multiple pages completing or starting) are batched together into a single disk write.

2.  **Update `src/services/translation/TranslationJobRunner.ts`**:
    *   Locate the `saveWithPriority` method.
    *   Increase the debounce delay for `BACKGROUND` priority to `30000` ms (30 seconds).
    *   **Fix Data Loss Bug**: Modify the debounce logic to accumulate changes. Currently, it overwrites `pendingSaveData` with the latest delta, which causes data loss if multiple updates happen within the debounce window. I will change it to either deep-merge the deltas or send the full local state (`this.translationMap`, etc.) when the timer fires, ensuring all changes are saved.

### **Verification:**
*   I will verify the code changes by reviewing the modified logic to ensure the debounce timer is correctly set and that the data payload is correctly constructed (accumulated or full state) to prevent data loss during the longer delay.
