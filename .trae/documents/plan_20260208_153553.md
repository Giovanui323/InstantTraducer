I have identified the critical bug causing your files to "disappear".

**The Problem:**
The application is encountering a **crash on startup** (specifically an "Attempted to register a second handler" error). This happens because the application tries to register its internal commands multiple times when the window loads. 
Because of this crash, the part of the app responsible for listing your saved files never runs, resulting in an empty list. **Your files are safe** in the application folder, but the app is unable to display them.

**The Fix:**
I need to modify the code to prevent these commands from being registered more than once. This involves updating three files:
1.  `electron/packageLogic.js`
2.  `electron/pdfHandlers.js`
3.  `electron/projectHandlers.js`

**Next Steps:**
1.  I will apply the code fixes.
2.  **Crucial:** Since you are using the *built* version of the app, **you will need to rebuild the application** (run `npm run package` again) after I apply the fixes. The old built application will remain broken until it is replaced by the new build.

Do you want me to proceed with these fixes?
