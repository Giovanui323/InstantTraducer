I will run the existing `build:all` script which is configured to:

1. Increment the patch version number (e.g., 3.5.9 → 3.5.10) using `npm version patch`.
2. Run the full build and packaging process using `npm run package`.

Command to execute:

```bash
npm run build:all
```

