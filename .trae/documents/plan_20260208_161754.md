I will address the issue of duplicate IPC calls at startup by disabling `React.StrictMode`. The "double mount" behavior observed is a deliberate feature of React Strict Mode in development (to detect side effects), but it causes redundant data fetching in this case.

### Plan:
1.  **Modify `src/index.tsx`**:
    *   Remove the `<React.StrictMode>` wrapper around the `<App />` component.
    *   This ensures the `App` component (and its `useAppLibrary` hook) mounts only once at startup, preventing the duplicate `get-translations` call.

2.  **Verify**:
    *   The change is purely within the renderer process and will be picked up by HMR (Hot Module Replacement) if running, or on the next reload.
    *   This will eliminate the 34ms spaced duplicate logs.
