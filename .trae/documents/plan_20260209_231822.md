I will fix the issue where highlighting text within notes does not work because the `selectableText` (used for highlight resolution and word boundary expansion) does not include the text content of the notes.

**Plan:**

1.  **Modify `src/components/MarkdownText.tsx`**:
    *   Update the `selectableText` calculation (inside `useMemo`) to append the text content of footnotes, user notes, and external notes.
    *   Ensure the constructed text string matches the structure of the rendered DOM (concatenating note number, word, and content with appropriate spacing, and adding newlines between note blocks).

**Implementation Details:**
*   Currently, `selectableText` is built only from `bodyText`.
*   I will extend it to append:
    *   Footnotes (if present and visible): `${n} ${text}`
    *   User Notes (if present and visible): `${n} ${selectedWord} ${noteContent}`
    *   External Notes (if present): `${n} ${word} ${comment}`
*   I will ensure that newlines are added between the main body and the note sections to match the block layout structure that `computeOffsets` detects in the DOM.

This change will ensure that when `expandToWordBoundaries` or `resolveHighlightsByQuote` is called with offsets corresponding to the notes area, valid text is returned instead of an empty string, allowing the highlight to be created and rendered correctly.