I will modify `src/hooks/useAppLibrary.ts` to address the issues of double saves and processing of blocked files.

The issues identified are:
1.  **Double Save / Identical Requests**: Caused by a race condition where `updateLibrary` relies on `recentBooksRef.current` which is updated asynchronously via a `useEffect`. Rapid successive calls (e.g., < 150ms) see stale data, causing the "no-change" check to fail and resulting in duplicate save requests.
2.  **Processing Blocked Files**: `processSaveQueue` lacks a check to ensure it doesn't process files that have been blocked or blacklisted (e.g., during deletion), which can lead to errors or inconsistencies.

**Proposed Changes in `src/hooks/useAppLibrary.ts`:**

1.  **Fix Stale State in `updateLibrary`**:
    -   Calculate the new state object synchronously within `updateLibrary`.
    -   Update `recentBooksRef.current` immediately, before calling `setRecentBooks`.
    -   This ensures that subsequent calls to `updateLibrary` (even in the same event loop) see the latest data and correctly deduplicate identical requests.

2.  **Add Blocked Check in `processSaveQueue`**:
    -   Inside the `processSaveQueue` loop, add a check for `blacklistedIdsRef` and `transitioningIdsRef`.
    -   If a file ID is blocked, skip the save operation and log a warning.

This approach directly addresses the user's reported problems and the specific instruction to modify `useAppLibrary.ts`.