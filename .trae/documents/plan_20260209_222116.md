The user has identified a critical issue where "Force Enqueue" operations (triggered by automatic retries or quality checks) bypass the global "Paused" state. This defeats the purpose of the Circuit Breaker/Pause mechanism, especially when the system is paused due to API errors (like 429 Quota Exceeded), causing the system to hammer the API even when it should be cooling down.

To fix this, I will modify `src/hooks/useTranslationQueue.ts` to ensure that **all** translation tasks, including "forced" ones, respect the global `isPaused` state.

### Plan Steps

1.  **Modify `src/hooks/useTranslationQueue.ts`**:
    *   In the `pumpQueue` function (around line 149), remove the exception that allows forced pages to bypass the `isPaused` check. The logic will be changed from `if (isPausedRef.current && !isForced) return false;` to `if (isPausedRef.current) return false;`.
    *   In the `enqueueTranslation` function (around line 292), update the log message. Instead of saying "bypass dello stato di pausa ATTIVO", it should log that the page is forced but waiting for the pause to be lifted (e.g., "aggiunto in cima (in attesa di ripresa dalla pausa)").

### Impact
*   **Automatic Retries**: When the system pauses due to errors (e.g., 429), automatic retries from quality checks will now be queued but will **not** execute until the system is unpaused. This allows the API quota to reset properly.
*   **Manual Retries**: If a user manually forces a page while the system is paused, it will also wait. The user must unpause the system to resume processing, which is consistent with a "Paused" state.
