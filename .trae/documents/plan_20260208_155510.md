I will modify `electron/main.js` to center the main window when it opens, addressing the user's observation that the window should be centered.

Regarding the "freedom of movement" issue:

* The "Translation in progress" view and "Translated page" (Reader View) share the same draggable Header.

* The perceived difference in movement "freedom" likely stems from the heavy rendering load in the Reader View (which renders PDF pages and canvases) compared to the lightweight Translation Activity Modal. Heavy main-thread activity can make window dragging feel less responsive ("laggy").

* Since performance optimization requires deeper profiling, I will focus on the clear request to center the window, which is a definite improvement.

**Proposed Changes:**

1. **Update** **`electron/main.js`**: Add `center: true` to the `BrowserWindow` configuration options. This ensures the application window appears in the center of the screen upon launch.

