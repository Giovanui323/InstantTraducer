I will implement the fix specifically targeting the fallback scenario where `gemini-2.5-flash` is used as a verifier.

### **1. Modify Prompt Logic (`src/services/prompts.ts`)**
-   Update `getVerifyQualitySystemPrompt` to accept `modelName`.
-   Add a specific check for `gemini-2.5-flash`.
-   When this model is detected, inject a **"Critical Reviewer Protocol"** into the system prompt. This protocol will explicitly instruct the model to:
    -   Ignore its default "helpful assistant" tendency.
    -   Focus exclusively on finding errors.
    -   Avoid rewriting/translating the text itself.
    -   **Forcefully verify** that it is not generating a "false positive" OK.

### **2. Update Verifier Service (`src/services/geminiService.ts`)**
-   Update the `verifyTranslationQualityWithGemini` function to pass the active `verifierModel` to the prompt generator.

This ensures that when the system falls back to `gemini-2.5-flash` due to quota limits on the primary model, the fallback model receives the necessary extra guidance to perform its job correctly, without affecting the primary `gemini-3-flash-preview` model (which presumably works correctly).
