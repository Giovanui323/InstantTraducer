I will modify `src/hooks/useAppLibrary.ts` to resolve the "Blocked cross-save" and "Invalid project data" errors occurring after file renaming.

The fix involves two key changes within `src/hooks/useAppLibrary.ts`:

1.  **Update `registerRename`**:
    -   Add logic to check if the `oldId` matches the currently active project (`currentProjectFileIdRef.current`).
    -   If it matches, immediately update the active project ID to the `newId`. This ensures the application knows the active session has moved to the new file, preventing "Blocked cross-save" errors that occur when the system thinks we are still on the old ID.

2.  **Enhance `processSaveQueue`**:
    -   Inside the processing loop, check if a request's `fileId` exists in `renamedIdsRef` (the redirection map).
    -   If a redirection is found:
        -   Update the request's `fileId` to the `newId`.
        -   Re-fetch `currentData` from `recentBooksRef` using the *new* ID. This ensures we get the correct, full project state (including `fileName`).
    -   This fixes the "Invalid project data" error, which happens when the queue tries to save using the old ID, finds no data in memory (since the old ID was removed), and attempts to send a partial payload missing the required `fileName`.

This approach handles the state transition atomically and ensures pending saves are correctly routed to the new file.