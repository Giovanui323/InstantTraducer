I will fix the two reported issues:

1.  **Blocked Save Attempts (Issue #2)**:
    *   **Problem**: The AI translation process holds a reference to the old Project ID. When the project is renamed (e.g., after title extraction), the old ID is blacklisted, causing the AI's save attempts to fail with "Blocked save attempt".
    *   **Fix**:
        *   Modify `src/hooks/useAppLibrary.ts` to introduce a `renamedIdsRef` map.
        *   Update `updateLibrary` to check this map: if a save targets a renamed ID, automatically redirect it to the new ID.
        *   Modify `src/hooks/useProjectManagement.ts` to register the rename mapping (`oldId` -> `newId`) immediately after a successful rename operation.

2.  **Ghost Duplicates & Consolidation (Issue #5)**:
    *   **Problem**: Dragging/dropping a PDF often creates a duplicate project instead of opening the existing one, leading to "Consolidation" logic running at startup to merge them. This happens because the Project ID generation depends on the file path, so moving/renaming the source file generates a new ID.
    *   **Fix**:
        *   Modify `src/App.tsx` in the `continueUploadWithLanguage` function.
        *   Enhance the duplicate detection logic to prioritize **File Fingerprint** (content hash) over File Path.
        *   If a project with the same fingerprint exists, open it directly (and update its path if needed) instead of creating a new one.
        *   Add path normalization (lowercase/separator unification) to improve path-based matching as a fallback.

This approach ensures robust saving during renames and prevents the creation of duplicate projects.