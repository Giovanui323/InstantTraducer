I will implement an automatic orphan asset recovery mechanism within the project loading process.

1.  **Modify `electron/projectHandlers.js`**:
    -   In the `load-translation` handler, locate the section where it checks for the existence of the original PDF (`localPdf`).
    -   If `localPdf` is not found, trigger a new recovery logic:
        -   Scan the `assets` directory for folders that match the project ID (e.g., `9782275088457_pdf` for project `9782275088457`).
        -   If a matching "orphan" folder is found and contains a valid PDF:
            -   **Scenario A (Target Missing/Empty)**: If the expected asset folder is missing or empty, rename the orphan folder to the expected name (restoring the link permanently).
            -   **Scenario B (Target Exists)**: If the expected folder exists and is not empty, copy the PDF from the orphan folder to the expected folder.
        -   Update the internal `localPdf` reference so the rest of the loading process proceeds as if the file was always there.
    -   Add logging to track when an orphan recovery occurs.

This change ensures that when you open a project like `9782275088457`, the system will automatically find and re-link the `9782275088457_pdf` folder, preventing the "Text Only" mode fallback and resolving the orphan status reported by the Deep Health Check.