I will implement a comprehensive fix for the three reported issues, focusing on data safety, resource management, and error handling.

### 1. Fix Silent Data Loss ("316 bytes" Mystery)
The root cause is a race condition in the Electron backend where a failed read of the existing project file (due to locking or I/O busy) defaults to an empty object `{}`. The subsequent save overwrites the entire project with just the new partial data (metadata), causing massive data loss.

**Changes:**
*   **Backend (`electron/projectHandlers.js`):** 
    *   Modify `save-translation` to **abort the save operation** if reading the existing file fails (unless the file truly doesn't exist). It will no longer default to `{}` on read errors.
    *   Add a safety check to prevent overwriting a large translation file with a tiny one (heuristic check).
*   **Frontend (`TranslationJobRunner.ts`):** 
    *   Update `flushPendingSave` to **restore the data to the buffer** if the save operation fails, ensuring no data is lost during temporary network/backend glitches.

### 2. Fix Queue Conflict (Translation vs Verification)
The two queues (`QUEUE` and `VERIF-QUEUE`) currently run independently, ignoring each other's resource usage. This allows them to exceed safe concurrency limits when running simultaneously (e.g., 2 translations + 2 verifications = 4 active tasks), leading to API bottlenecks and stalls.

**Changes:**
*   **New Service (`src/services/concurrencyManager.ts`):** Create a global singleton to manage a shared pool of execution slots.
*   **Update Hooks:** Modify `useTranslationQueue.ts` and `useVerificationQueue.ts` to acquire and release slots from this global manager instead of using local counters. This guarantees that `Translations + Verifications` never exceeds the global limit (default 2).

### 3. Fix "Ghost" Circuit Breaker
The Circuit Breaker never trips because the translation executor catches all errors (to update the UI with a red status) but swallows them instead of re-throwing. The queue manager sees every task as "successful" (completed without throwing), so it never counts failures.

**Changes:**
*   **Executor (`src/services/translation/TranslationExecutor.ts`):** Modify the error handling logic to **re-throw critical errors** (like `BLOCKED_BY_BACKEND`, network errors, or API failures) after updating the UI status.
*   **Result:** The `useTranslationQueue` will now correctly detect the failure, increment the failure count, and trip the Circuit Breaker after 3 consecutive errors as intended.

I will verify these fixes by simulating save failures and checking queue behavior.