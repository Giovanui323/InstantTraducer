I have identified the issue. The "Salvataggio..." banner gets stuck because the application correctly starts the saving process and turns on the indicator, but it **forgets to turn it off** after the save is successfully completed.

**Does it save?**
**Yes, it does.** The actual saving logic (`window.electronAPI.saveTranslation`) is executed correctly. The data is safe; only the visual "Saving..." label is bugged and stays visible indefinitely.

### Plan to Fix
I will modify `src/hooks/useAppLibrary.ts` to ensure the banner is hidden once the save queue is empty.

1.  **Modify `processSaveQueue`**: Add a check at the end of the save processing function.
2.  **Logic**: If there are no more pending items in the `saveQueue`, call `setIsSaving(false)` to hide the banner.

This is a safe, targeted fix that resolves the UI issue without affecting the actual data saving integrity.