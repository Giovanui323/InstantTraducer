I will fix the settings loading issue by removing the unnecessary delay and improving error logging.

1. **Remove Debounce Delay for Loading Settings**:

   * In `electron/settingsLogic.js`, I will update the `load-settings` IPC handler to use `{ priority: 'CRITICAL' }` when calling `settingsMutex.enqueue`. This ensures that loading settings happens immediately instead of waiting for the default 2-second debounce (which is intended for saving, not loading).

2. **Improve Error Logging**:

   * In `src/hooks/useAiSettings.ts`, I will update the error logging in the `load` function to explicitly log the error message or stringified error object. This will help identify the exact cause of the "Errore caricamento impostazioni" if it persists, as "Object" is currently uninformative.

3. **Verify**:

   * I will verify the changes by checking the code. I cannot run the app to verify the fix directly, but these changes are safe and address the identified issues (unnecessary delay and poor logging).

