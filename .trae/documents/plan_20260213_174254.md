I will fix the "New Upload Paradox" where a newly uploaded file is treated as a "Recovery" because it is temporarily missing from the library state. This is caused by a race condition in the `refreshLibrary` function, which fails to preserve the active in-memory project if it hasn't been written to disk yet.

### Plan
1.  **Modify `src/hooks/useAppLibrary.ts`**:
    *   Update the `refreshLibrary` function to fix the state merge logic.
    *   Specifically, when merging the disk state (`next`) with the current state (`prev`), add a check to ensure that if the active project exists in `prev` (in-memory) but is missing from `next` (disk), it is preserved instead of being discarded.
    *   This prevents the "background refresh" from wiping out a new upload that is currently in memory but not yet flushed to disk.

### Verification
*   I will verify the fix by checking the code changes in `useAppLibrary.ts`.
*   Since this is a race condition that is hard to reproduce deterministically in a test environment without mocking the file system and timing, I will rely on the logic correction which directly addresses the identified flaw.
