I will implement a multi-layered defense against the "Leaky Semaphore" (Zombie Slots) and "Hanging Task" issues, ensuring that concurrency slots are always released even if the underlying AI processes hang.

### 1. "Safety Valve" in `useTranslationQueue.ts` (The Ultimate Fix)
I will modify `src/hooks/useTranslationQueue.ts` to wrap the `processPage` execution in a strict "Safety Timeout" race.
- **Action**: In `pumpQueue`, wrap `await processPageRef.current(...)` with a `Promise.race` against a timeout (e.g., 3 minutes).
- **Logic**: If the timeout wins, log a critical error "Leaky Semaphore Detected: Force-releasing slot for page X" and proceed to the `finally` block to decrement `activePagesRef`.
- **Reason**: This ensures that *no matter what happens* in the executor (infinite loop, ignored abort signal, network hang), the concurrency slot will be freed, preventing the "2/2 stuck" scenario.

### 2. Timeout for Quality Check in `TranslationExecutor.ts`
I will modify `src/services/translation/TranslationExecutor.ts` to apply a timeout to the verification phase, which was previously unprotected.
- **Action**: Wrap the `verifyAndMaybeFixTranslation` call (line 450) with `withTimeout` (using `AI_TRANSLATION_TIMEOUT_MS`).
- **Reason**: The user logs indicated a hang during phases that didn't output logs, likely the verification phase. This prevents the hang at the source.

### 3. Hard Reset & Home Consistency
I will modify `src/App.tsx` to enforce a clean slate when returning to Home.
- **Action**: In `resetProjectState`, ensure `translation.abortAll(true)` is called *before* clearing local state, and verify `resetQueue` is effective.
- **Reason**: This ensures that when the user leaves the project, any "zombie" processes are killed and their slots cleared.

### 4. Hardening `useAppTranslation` (Previous Plan)
I will still include the guard against redundant rendering.
- **Action**: Add `if (translationMapRef.current[page]) return;` in `processPage` in `src/hooks/useAppTranslation.ts`.
- **Reason**: Prevents redundant processing of already-translated pages.

### Summary
This plan directly addresses the user's detailed analysis:
- **"Leaky Semaphore"**: Fixed by the Safety Valve in `useTranslationQueue`.
- **"Gap di 2 minuti"**: Fixed by adding timeout to Verification phase.
- **"Retry forzato fallito"**: Fixed by ensuring slots are actually freed.
- **"Home Incoerenza"**: Fixed by enforcing hard reset.