I have analyzed `src/App.tsx` and identified the cause of the numerous errors. There are two duplicate code blocks that have been pasted incorrectly, causing syntax errors (unbalanced braces) and scope issues.

### Issues Identified
1.  **Duplicate Block 1 (Lines 1009-1016)**: A block of code setting the current page and logging a warning is duplicated. The duplicate contains an extra closing brace `}` which closes the function scope prematurely, causing "Cannot find name" errors for subsequent variables.
2.  **Duplicate Block 2 (Lines 1131-1208)**: A large block of code handling PDF loading is duplicated after a `catch` block. This orphaned code sits between a `try` block and its corresponding `catch`, causing "catch or finally expected" errors and further scope confusion.

### Plan
1.  **Remove Duplicate Block 1**: Delete lines 1009-1016 in `src/App.tsx`.
2.  **Remove Duplicate Block 2**: Delete lines 1131-1208 in `src/App.tsx`.

These changes will restore the correct code structure, fix the brace balancing, and resolve the reported diagnostics.