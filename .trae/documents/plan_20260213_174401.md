I will update the technical report to include the 4 new critical inconsistencies identified, integrating them with the previous 3 points.

### **Updated Technical Report Structure**

I will create a comprehensive document `Report_Tecnico_Completo.md` covering all 7 issues, with detailed technical analysis and proposed fixes for the new points.

#### **Part 1: Structural Criticalities (New Findings)**

1.  **Silent Data Loss ("316 Byte Mystery")**
    *   **Issue:** `save-translation` IPC handler saves a 316-byte file (metadata only) despite receiving large translation content, implying `mergeProjectData` or the payload transmission is failing silently.
    *   **Technical Root:** In `electron/projectHandlers.js`, the `save-translation` handler likely receives an empty/malformed `data` object or `mergeProjectData` wipes the translations array. There is no size validation on the *output* JSON before writing.
    *   **Proposed Fix:** Add a "Safe Save" check in `projectHandlers.js`: if the new content has significantly fewer translations than the existing file, block the save and log a critical error.

2.  **Queue Resource Conflict (Translation vs Verification)**
    *   **Issue:** `[QUEUE]` and `[VERIF-QUEUE]` compete for the same Node.js event loop resources. Both report "2/2" active slots, causing resource saturation.
    *   **Technical Root:** While they have separate counters (`MAX_CONCURRENT_TRANSLATIONS` vs `_VERIFICATIONS`), they run in the same Renderer thread. Heavy processing in one blocks the other.
    *   **Proposed Fix:** Implement a **Shared Concurrency Semaphore** or a "Yield" system where Verification pauses if Translation is active.

3.  **Phantom "Circuit Breaker"**
    *   **Issue:** Circuit Breaker never trips despite 8+ `BLOCKED_BY_BACKEND` errors because it is reset too frequently.
    *   **Technical Root:** The `resetQueue` function (called during auto-recovery or component re-mounts) blindly clears the `globalCircuitBreakerRegistry`. Also, `BLOCKED_BY_BACKEND` errors might be caught as "soft errors" and not trigger `recordFailure`.
    *   **Proposed Fix:** Decouple `CircuitBreaker` reset from `Queue` reset. Make `BLOCKED_BY_BACKEND` explicitly increment the failure counter.

4.  **IPC Memory Saturation (15MB Buffer)**
    *   **Issue:** `save-original-pdf-buffer` sends 15MB+ raw buffers via IPC, blocking the Main process.
    *   **Technical Root:** `electron/projectHandlers.js` accepts a raw `buffer` argument. Structuring cloning large buffers is synchronous and expensive in Electron.
    *   **Proposed Fix:** Refactor to pass the **file path** string instead of the buffer. Let the Main process read the file directly from disk (`fs.promises.copyFile`).

#### **Part 2: Logic & AI Issues (Previous Findings)**

5.  **Page 150 Hallucination:** AI confusing citations with content. (Fix: Prompt Engineering).
6.  **Race Condition:** Project switching overwrites data. (Fix: 2s Safety Shield in `useAppLibrary`).
7.  **Infinite Retry Loop:** Verification loop on false positives. (Fix: "Force Save" on exhaustion).

---

### **Action Plan**
1.  **Create Report**: Generate `Report_Tecnico_Completo.md` with the content above.
2.  **Wait for Approval**: I will wait for your confirmation to proceed with the actual code fixes (starting with the "316 Byte" data loss protection).