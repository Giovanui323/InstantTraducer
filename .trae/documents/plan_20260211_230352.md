I have analyzed the provided logs and the `useTranslationQueue.ts` source code. The "STALLED" warning appearing in your logs is a **false positive** caused by a logic flaw in the queue management.

**The Issue:**
The system reports a "STALLED" state whenever no page in the *waiting queue* can be started immediately (e.g., because they are waiting for the previous page to finish). However, it fails to check if there are **active pages currently running**. As long as there is at least one active translation (like Page 9 in your logs), the system is *not* stalled; it is simply processing.

**The Fix:**
I will modify `src/hooks/useTranslationQueue.ts` to only report a "STALLED" state if:

1. No pages in the queue can be started (dependencies not met).
2. **AND** there are no active pages running (`activePagesRef.current.size === 0`).

If there are active pages, the queue is correctly "Waiting", not "Stalled".

**Plan:**

1. Edit `src/hooks/useTranslationQueue.ts`.
2. Locate the logic block responsible for the `[QUEUE] STALLED` warning (around line 270).
3. Add a check for `activePagesRef.current.size > 0`.

   * If active pages exist, log an `INFO` message (e.g., "Queue waiting for active tasks...") instead of a `WARNING`.

   * Prevent the "Zombie Check" logic from running when the system is merely waiting, as this could lead to incorrect state cleanup.
4. Verify the fix by reviewing the code logic (as direct runtime reproduction requires complex timing).

