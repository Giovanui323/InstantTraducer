I will implement a robust Configuration Validation and Feature Flag system to ensure system stability and consistency.

### 1. Configuration Validation System (`src/services/configValidation.ts`)
I will create a dedicated service to validate `AISettings` before they are applied or saved.
- **Validation Rules**:
  - **Provider Integrity**: Ensure `provider` is valid ('gemini' or 'openai').
  - **Model Verification**: Validate `gemini.model` against known valid models (e.g., `gemini-3-pro-preview`, `gemini-3-flash-preview`).
  - **Conflict Resolution**: Detect and resolve conflicts (e.g., if `fastMode` is active, ensure a Flash model is selected; if `qualityCheck` is enabled, ensure `verifierModel` is valid).
  - **Safe Defaults**: Provide a fallback mechanism to return to safe defaults if critical settings are missing or invalid.
- **Integration**:
  - Integrate `validateSettings` into `useAiSettings` to validate on load and save.
  - Integrate into `SettingsModal` to provide real-time feedback (warnings/errors) to the user.

### 2. Feature Flag System (`src/services/featureFlags.ts`)
I will implement a centralized Feature Flag system to manage feature availability.
- **Feature Management**:
  - Create a `FeatureManager` to check if specific features (e.g., `QUALITY_CHECK`, `CONSULTATION_MODE`, `ADVANCED_LOGGING`) are enabled.
  - Allow toggling features via `AISettings` or internal defaults.
- **Consistency**:
  - Ensure feature flags respect the current configuration (e.g., disable `QUALITY_CHECK` if the provider doesn't support it).

### 3. Refactoring & Fixes
- **`src/services/aiService.ts`**:
  - Update `verifyTranslationQuality` to use `settings.qualityCheck.verifierModel` instead of hardcoded `GEMINI_VERIFIER_MODEL`.
  - Ensure all AI calls respect the validated settings hierarchy (Override > Settings > Default).
- **`src/hooks/useAiSettings.ts`**:
  - Implement validation logic during the settings load/save lifecycle.
  - Warn users about invalid configurations or auto-correct them where appropriate.

### 4. User Interface Updates
- Update `SettingsModal` to display validation warnings if the user selects an invalid combination of settings (e.g., incompatible model for the selected mode).

This approach ensures that the application runs with a consistent and valid configuration, preventing runtime errors due to misconfiguration.