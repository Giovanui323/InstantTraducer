I will modify `src/hooks/useAppLibrary.ts` to prevent the save queue from processing blocked or blacklisted files, which can cause memory saturation and system instability.

1.  **Update `processSaveQueue` in `src/hooks/useAppLibrary.ts`**:
    - Add a check inside the processing loop to verify if the `request.fileId` is present in `blacklistedIdsRef.current` or `transitioningIdsRef.current`.
    - If the ID is blocked:
        - Log a warning (e.g., "Skipping blocked save for ID...").
        - Skip the `electronAPI.saveTranslation` call.
        - **Do not** push the request back to the queue for retry.
    - This ensures that even if a save process is already running when a block occurs (race condition), it will respect the block immediately and stop "resurrecting" failed saves.

This change directly addresses the user's hypothesis that the "queue of failed saves" caused the internal collapse.
