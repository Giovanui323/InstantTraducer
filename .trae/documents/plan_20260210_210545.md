I will optimize the translation concurrency to prevent blocked pages (waiting for context) from occupying execution slots.

1.  **Modify `src/hooks/useTranslationQueue.ts`**:
    -   Implement a `SlotManager` mechanism with `acquireSlot` and `releaseSlot` functions.
    -   `releaseSlot(page)`: Removes the page from `activePagesRef` (freeing the slot) and triggers either a waiting task (from `slotRequestQueue`) or pumps the main queue.
    -   `acquireSlot(page)`: Checks if a slot is available. If yes, marks page as active. If no, suspends execution until a slot is freed.
    -   Pass a `concurrencyControl` object (containing these functions) to the `processPage` callback.

2.  **Modify `src/hooks/useAppTranslation.ts`**:
    -   Update `processPage` to accept the new `concurrencyControl` argument.
    -   Pass this control object to `executePageTranslation`.

3.  **Modify `src/services/translation/TranslationExecutor.ts`**:
    -   **Optimization**: Move the "Pipelining Synchronization" block (waiting for previous page) to the **beginning** of the function, *before* the expensive PDF rendering. This reduces memory usage as blocked pages won't render images until they are ready to translate.
    -   **Concurrency**: Wrap the synchronization wait loop with `concurrencyControl.release(targetPage)` and `await concurrencyControl.acquire(targetPage)`.
    -   This ensures that while a page is waiting for its predecessor, it yields its "AI Slot" to other independent pages (e.g., later chapters or non-sequential tasks), maximizing throughput.

This approach resolves both the "wasted AI slot" issue and the potential "memory saturation" by deferring resource allocation until the dependency is met.