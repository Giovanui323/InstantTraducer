I will modify `src/App.tsx` to ensure that pending data—specifically Quality Check verdicts and retranslation states—is persisted to disk when the user clicks "Close Session".

**Steps:**

1. **Locate** **`onCloseSession`** **in** **`src/App.tsx`**:

   * Currently, this function immediately resets the project state (`resetProjectState`), wiping out in-memory data before it has a chance to be saved to the JSON file.

2. **Implement Async Save Logic**:

   * Convert `onCloseSession` to an `async` function.

   * Inject a call to `await library.flushSaves()` *before* the state reset.

   * This forces any pending updates (like the "SEVERE" status and "autoRetryActive" flag generated by `useAppQuality`) to be written to the project's JSON file immediately.

3. **Add Logging**:

   * Add a log message to confirm that the flush operation is occurring during session closure.

**Outcome:**

* When a "SEVERE" error occurs and triggers a retranslation, the system updates the internal state.

* If the user immediately closes the session, `flushSaves()` ensures this state (marking the page as problematic/pending retry) is written to disk.

* On the next load, the project will contain the record of the failed page, preventing the "data loss" scenario described.

**Verification:**

* I will verify that the `onCloseSession` handler is correctly updated and that the `flushSaves` method is available in the scope (it is part of the `library` object).

