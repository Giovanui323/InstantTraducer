I will modify `src/components/ReaderView.tsx` to implement automatic A4 format detection and application for translated pages.

### Plan

1.  **Define A4 Standards**:
    -   Create constants for A4 Portrait (595x842) and A4 Landscape (842x595) dimensions (based on standard 72 DPI PDF points).

2.  **Update `frameDims` Calculation**:
    -   Modify the `frameDims` `useMemo` hook in `ReaderView.tsx`.
    -   When `isTranslatedMode` is active, instead of using the raw PDF dimensions, determine the "Global Frame" based on A4 standards.
    -   Logic: Scan all available page dimensions. If any page is detected as Landscape (width > height), set the global frame to A4 Landscape width (to accommodate the widest pages). Otherwise, use A4 Portrait.

3.  **Standardize Page Dimensions in Render Loop**:
    -   Inside the `renderPages.map` loop in `ReaderView.tsx`, intercept the `dims` calculation.
    -   When `isTranslatedMode` is active:
        -   Calculate the aspect ratio of the original source page.
        -   If ratio > 1.05 (Landscape), force dimensions to **A4 Landscape** (842x595).
        -   Otherwise, force dimensions to **A4 Portrait** (595x842).
    -   This ensures the translation container always follows a standard A4 format, regardless of the specific crop or scale of the source PDF.

4.  **Verify Vertical Flexibility**:
    -   Confirm that existing `dynamicStyles.container` logic uses `min-height` for translations (already verified in analysis), allowing the text to expand vertically beyond A4 height if the translation is longer than the page.

### Outcome
The translation view will now consistently present pages as standard A4 sheets (Vertical or Horizontal) based on the original page's orientation, providing a cleaner and more standardized reading experience, while still allowing text to flow naturally and extend downwards if needed.