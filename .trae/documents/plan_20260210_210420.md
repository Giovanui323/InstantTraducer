Based on the analysis of `src/hooks/useAppLibrary.ts` and `electron/validation.js`, I have confirmed that the **Blocked cross-save** errors are actually evidence that your safety mechanisms are **working correctly**. They are preventing data corruption during project switching.

 However, the **"fileName missing"** errors indicate a "latent" race condition where a background save attempts to process a partial update for a project that has already been unloaded from memory.

Here is the plan to resolve the errors and harden the system:

### 1. Fix "fileName missing" Error
**File:** `src/hooks/useAppLibrary.ts`
- **Action:** Modify `processSaveQueue` to validate `payload.fileName` before calling Electron.
- **Reason:** Currently, if `recentBooksRef.current` does not contain the project (because it was just unloaded), the code falls back to `request.data`. If this data is a partial update (e.g., just a page change), it lacks `fileName`, causing the backend validation failure.
- **Fix:** If `fileName` is missing, we will abort the save and log a warning instead of letting it fail in the backend.

### 2. Verify "Blocked cross-save" Logic
- The current logic in `updateLibrary` correctly compares the active project ID with the save target.
- I will add a comment or minor log adjustment to clarify that this "Error" is a **successful interception** of a ghost write.

### 3. Implementation Steps
1.  **Edit `src/hooks/useAppLibrary.ts`**:
    - In `processSaveQueue`, add the `!payload.fileName` check.
    - Add a log message: `Skipping save: Missing fileName for ID ${request.fileId} (orphaned partial update)`.
2.  **Verify**: Run the application (or ask you to check logs) to confirm the "Dati progetto non validi" error disappears during rapid switching.

This approach eliminates the log noise and handles the "latent" misalignment gracefully without risking data loss.