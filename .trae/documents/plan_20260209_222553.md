I will enable the right-click context menu (Copy/Cut/Paste) in the Electron application, which is currently missing.

**1. Modify `electron/main.js`**
- Import `Menu` from the `electron` package.
- In the `createWindow` function, add a listener for the `context-menu` event on `mainWindow.webContents`.
- Implement a context menu that appears on right-click, providing options for:
    - **Copia** (Copy) - Enabled when text is selected.
    - **Taglia** (Cut) - Enabled in editable fields.
    - **Incolla** (Paste) - Enabled in editable fields.
    - **Seleziona tutto** (Select All).
    - **DevTools/Reload** (Only in development mode).

This change will allow the native system context menu to appear when right-clicking in the Reader (and throughout the app), enabling the "Copy" functionality as requested.