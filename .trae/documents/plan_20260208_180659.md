1. **Reduce Idle Threshold**: Change the idle detection in `App.tsx` from 120,000ms (2 mins) to 60,000ms (1 min) for faster suspension of background tasks.
2. **Fix Dependency Arrays**: Add `isFocused` and `isIdle` to the `useEffect` hooks for `scanDims` and the auto-prefetcher in `App.tsx`. This ensures background processes react instantly when the window loses focus or the user goes idle.
3. **Optimize scanDims Persistence**:

   * Increase the throttle between background batches in `scanDims` from 3s to 15s.

   * Implement an accumulation logic: instead of calling `updateLibrary` every 5 pages, accumulate page dimensions and only trigger a disk save every 30 pages or when the scan completes.
4. **Refine Prefetcher Stability**: Ensure the auto-prefetcher's `useEffect` correctly clears its timer and stops execution as soon as `isFocused` or `isIdle` state changes.

